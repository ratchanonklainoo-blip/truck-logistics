# QA — ฟีเจอร์ "วางลิงก์ Google Maps แทนกรอกพิกัด"

**ผู้ตรวจ:** Fern (QA Consultant) · **Dev:** Dev1 · **วันที่:** 1 กันยายน 2026
**สรุป:** ✅ **พร้อม push** — 6/6 เคสผ่าน + SSRF/auth guard ผ่าน · มี 3 ข้อควรแก้ตาม (ไม่บล็อก)

**วิธีทดสอบ:** dev server จริง (`npm run dev`) + **ล็อกอินจริง**ด้วย test user (`qa.fern.disposable.…`) ผ่าน headless Chrome (CDP) → แตะ Supabase production จริง · ทุกผลด้านล่างมาจากหน้าเว็บจริง ไม่ใช่ stub
**ระดับหลักฐาน:** 🟢 [เว็บจริง] · 🔵 [API จริง] · 🟡 [โค้ด]

> **หมายเหตุความโปร่งใส:** รอบแรกล็อกอินไม่ผ่าน เพราะ `.next` cache เสีย (JS chunks ตอบ 404 → React ไม่ hydrate → ฟอร์มกดไม่ทำงาน) **ไม่ใช่บั๊กของแอป** — ลบ `.next` แล้วรันใหม่ ล็อกอินผ่านทันที

---

## ตารางสรุป PASS/FAIL

| # | จุดทดสอบ | ผล | หลักฐาน |
|---|---|---|---|
| 1 | ลิงก์เต็ม `?q=lat,lng` → parse ทันที | ✅ **PASS** | 🟢 [เว็บจริง] |
| 2 | ลิงก์ `@lat,lng,zoom` → parse ทันที | ✅ **PASS** | 🟢 [เว็บจริง] |
| 3 | พิกัดดิบ `lat,lng` | ✅ **PASS** | 🟢 [เว็บจริง] |
| 4 | ลิงก์ย่อนำทาง `iU2UUamf5qFk24sx6` → **13.723162, 100.739220** | ✅ **PASS** | 🟢 [เว็บจริง] |
| 5 | ลิงก์ย่อ Barrio Taco (ตปท.) → พังอย่างมีเหตุผล | ✅ **PASS** | 🟢 [เว็บจริง] |
| 6 | ลิงก์ย่อ ไก่ยกครก (ไทย) → พังอย่างมีเหตุผล | ✅ **PASS** | 🟢 [เว็บจริง] |
| 7 | SSRF guard (8 รูปแบบโจมตี) | ✅ **PASS 8/8** | 🔵 [API จริง] |
| 8 | auth guard — ไม่ล็อกอิน | ✅ **PASS (401)** | 🔵 [API จริง] |
| 9 | ข้อความมั่ว → เตือนชัด ไม่ค้างเงียบ | ✅ **PASS** | 🟢 [เว็บจริง] |
| 10 | ปุ่มบันทึกระหว่างรอ resolve | ⚠️ **PASS มีเงื่อนไข** — บล็อกได้จริง แต่ปุ่มไม่ถูก disable | 🟢 [เว็บจริง] |
| + | ครบ 3 หน้า (`/jobs-nearby`, `/drivers`, `/jobs`) | ✅ **PASS** | 🟢 [เว็บจริง] |

---

## รายละเอียดต่อเคส

### เคส 1-3 — ลิงก์เต็ม / `@lat,lng,zoom` / พิกัดดิบ 🟢
วางแล้วขึ้นพิกัดสีเขียวทันที **ไม่ต้องรอ resolve** (ไม่เห็น spinner = ถูกต้อง ไม่ยิงเซิร์ฟเวอร์โดยไม่จำเป็น)

| อินพุต | ผลบนหน้าเว็บ |
|---|---|
| `https://www.google.com/maps?q=19.910500,99.840600` | `19.910500, 99.840600` + ลิงก์ "เปิดแผนที่" |
| `https://www.google.com/maps/@19.108231,100.067771,17z` | `19.108231, 100.067771` |
| `19.9105,99.8406` | `19.910500, 99.840600` |

ภาพ: `r3-ui-1-fulllink.png` · `r3-ui-2-atlink.png` · `r3-ui-3-raw.png`

### เคส 4 — ลิงก์ย่อ "นำทาง" 🟢 ตรงเป๊ะตามที่ Dev1 ระบุ
`https://maps.app.goo.gl/iU2UUamf5qFk24sx6`
1. เห็นข้อความ **"กำลังแปลงลิงก์ย่อ..."** ระหว่างรอจริง (ยืนยันแล้ว)
2. ได้ผล **`13.723162, 100.739220`** = ค่าที่ Dev1 คาดไว้ทุกหลัก ✅
3. เวลา resolve จริง: **905–1004 ms** (ยิงซ้ำ 2 ครั้ง 200 ทั้งคู่)

> ข้อสังเกตทางเทคนิค: ลิงก์นำทางฝังพิกัดปลายทางเป็น `!1d{lng}!2d{lat}` (สลับ lng/lat) ซึ่ง `parseGoogleMapsLink` รองรับไว้ถูกต้องแล้ว — ถ้าไม่มี branch นี้จะได้ `@13.6860887,100.731166` ซึ่งเป็น**จุดกึ่งกลางแผนที่ ห่างจากปลายทางจริง ~4.2 กม.** ดิฉันทดสอบยืนยันจุดนี้แยกแล้ว ว่าโค้ดที่ส่งมาเลือกค่าถูก

ภาพ: `r3-ui-5-shortnav.png` (`/jobs-nearby`) · `r3-ui-9-drivers-shortlink.png` (`/drivers`) · `r3-ui-10-jobs-modal.png` (`/jobs`)

### เคส 5-6 — ลิงก์ร้าน (place ไม่มีพิกัดฝัง) — พังอย่างมีเหตุผลจริง 🟢

| ลิงก์ | HTTP | ข้อความบนหน้าเว็บ |
|---|---|---|
| `4Nh2nJoUi7kdZidCA` (Barrio Taco, สหรัฐฯ) | **422** | 🔴 "แปลงลิงก์ไม่สำเร็จ — ไม่พบพิกัดในหน้าเป้าหมาย" |
| `qWVEfMYfyYT1hyvR8` (ไก่ยกครก พัทยา) | **422** | 🔴 ข้อความเดียวกัน |

ทั้งคู่ **ขึ้นข้อความแดงจริงบนหน้าเว็บ ไม่ค้างเงียบ ไม่หมุนค้าง** ✅
สาเหตุยืนยันแล้ว: URL ปลายทางมีแค่ place ID (`/data=!4m2!3m1!1s0x89b8d760f7fd64e7:…` และ `?q=<ชื่อร้าน>&ftid=…`) ไม่มีตัวเลขพิกัดให้ parse — ตรงกับที่ Dev1 อธิบาย เป็นข้อจำกัดที่ยอมรับ ไม่ใช่บั๊ก

ภาพ: `r3-ui-6-place-en.png` · `r3-ui-7-place-th.png`

**ความเสถียร:** ยิงเคส 6 ซ้ำ 4 ครั้ง → 422 ถูกต้อง 3 ครั้ง, ล้มกลางทาง 1 ครั้ง (`Failed to fetch` ที่ 571ms) เป็นอาการของ dev server ที่กำลัง recompile ไม่ใช่ตรรกะฟีเจอร์ — ถ้าเกิดบน production ผู้ใช้จะเห็น "เชื่อมต่อเพื่อแปลงลิงก์ไม่สำเร็จ" ซึ่งรับได้ ควรดูซ้ำหลัง deploy

### เคส 9 — ข้อความมั่ว 🟢
วาง `ไปส่งของที่ลำปางนะครับ` → ขึ้นทันที:
> "รูปแบบไม่ถูกต้อง — วางลิงก์ Google Maps หรือพิกัด lat,lng เช่น 19.108231,100.067771"

มีตัวอย่างรูปแบบที่ถูกต้องประกอบ (ตรงหลัก Nielsen #9 help users recognize & recover) ✅ ภาพ: `r3-ui-4-garbage.png`

### เคส 7 — SSRF guard: ผ่านครบ 8/8 🔵
ยิงจริงผ่าน `/api/resolve-maps-link` ด้วย cookie ที่ล็อกอินแล้ว — **ทุกอันถูกปฏิเสธที่ 400 ก่อน fetch**

| URL ที่ยิง | ผล |
|---|---|
| `http://127.0.0.1:3000/api/jobs` (ยิงตัวเอง) | 400 ✅ |
| `http://169.254.169.254/latest/meta-data/` (cloud metadata) | 400 ✅ |
| `https://example.com/` | 400 ✅ |
| `https://maps.app.goo.gl.evil.com/abc` (subdomain หลอก) | 400 ✅ |
| `https://maps.app.goo.gl@evil.com/abc` (userinfo หลอก) | 400 ✅ |
| `https://evil.com/maps.app.goo.gl/abc` (path หลอก) | 400 ✅ |
| `file:///C:/Windows/win.ini` | 400 ✅ |
| `https://www.google.com/maps?q=19.9,99.8` (ไม่ใช่ลิงก์ย่อ) | 400 ✅ |

ข้อความปฏิเสธ: `ไม่ใช่ลิงก์ Google Maps แบบย่อที่รองรับ (maps.app.goo.gl หรือ goo.gl/maps)`
whitelist บังคับ `^https?://(maps\.app\.goo\.gl|goo\.gl/maps)/` — ปิดช่อง `@` และ subdomain ได้ถูกต้อง

### เคส 8 — auth guard 🔵
`POST /api/resolve-maps-link` โดยไม่ส่ง cookie → **401 `{"error":"Unauthorized"}`** ✅ (ยืนยันซ้ำทั้ง curl และ `credentials:'omit'` ในเบราว์เซอร์)

### เคส 10 — ล็อกปุ่มบันทึกระหว่างรอ resolve ⚠️ PASS มีเงื่อนไข 🟢
**บล็อกการบันทึกได้จริง — ไม่มีทางบันทึกพิกัด null/เก่า:**
- `/jobs-nearby` + `/drivers`: กด "บันทึกตำแหน่ง" ระหว่างยัง resolve ไม่เสร็จ → เด้ง alert *"รูปแบบไม่ถูกต้อง หรือกำลังแปลงลิงก์ยังไม่เสร็จ — …"* และ **ไม่เขียนลง DB**
- `/jobs`: กด "บันทึก" ระหว่างรอ → modal ไม่ปิด และ **จำนวนงานก่อน/หลัง = 0 เท่ากัน ไม่มีงานถูกสร้าง** ✅ (ภาพ `r3-ui-11-jobs-blocked-save.png`)
- เปลี่ยนข้อความในช่องระหว่างรอ → ผลลัพธ์เก่าถูกทิ้ง (มี `requestSeq` กันผลลัพธ์ข้ามคิว) 🟡 [โค้ด]

**แต่ปุ่มยัง `ENABLED` ตลอดช่วง resolve** — ผู้ใช้กดได้แล้วเจอ alert แทนที่จะเห็นปุ่มถูกล็อก (ดูข้อ F2)

---

## สิ่งที่ควรแก้ (ไม่บล็อก push)

### 🟠 F1 — User-Agent มือถือที่ hardcode ทำให้ลิงก์บางประเภทตอบ error ผิดประเภท
`route.ts` ส่ง UA `Mozilla/5.0 (Linux; Android 13) …` เมื่อ fetch ลิงก์ย่อ
Google จึงตอบกลับเป็น **`intent://…` (deep link ของ Android)** สำหรับลิงก์บางแบบ → `fetch` ของ Node โยน `URL scheme must be a HTTP(S) scheme` → route ตกไปที่ `catch` คืน **502 "เชื่อมต่อเพื่อแปลงลิงก์ไม่สำเร็จ กรุณาลองใหม่"**

ทดสอบยืนยัน (ลิงก์ร้านในเชียงราย `oPqZ3UQMXwFDFjbb6`, ซ้ำ 2 ครั้งได้ผลเดิม):

| UA | ผล |
|---|---|
| UA มือถือ (โค้ดปัจจุบัน) | ❌ throw → **502 "กรุณาลองใหม่"** (ลองใหม่กี่ครั้งก็ไม่หาย) |
| UA เดสก์ท็อป | ✅ 200 resolve ได้ → แล้วค่อยตอบ 422 ตามจริง |

**ผลกระทบ:** ผู้ใช้เจอข้อความชวนเข้าใจผิดว่าเน็ตมีปัญหา และกดซ้ำวนไปเรื่อยๆ
**แก้:** เปลี่ยน UA เป็นเดสก์ท็อป (แก้บรรทัดเดียวใน `route.ts`) — `'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120 Safari/537.36'`
เผื่ออนาคต: เพิ่ม `AbortSignal.timeout(8000)` กัน fetch ค้าง

### 🟡 F2 — ปุ่มบันทึกไม่ถูก disable ระหว่างรอ resolve
Nielsen #1 (visibility of system status): ปุ่มที่กดได้แต่กดแล้วโดนด่า = ผู้ใช้งง
**แก้:** ให้ `CoordPasteInput` ส่งสถานะ `resolving` ออกมา (`onResolvingChange`) แล้วผูก `disabled={saving || resolving}` ทั้งปุ่ม "บันทึกตำแหน่ง" และปุ่ม "บันทึก" ที่ `/jobs` + เปลี่ยนข้อความปุ่มเป็น "กำลังแปลงลิงก์..." — ผู้ใช้จะไม่ต้องเจอ alert เลย

### 🟡 F3 — ข้อความ 422 ยังไม่บอกว่าให้ทำอะไรต่อ
"แปลงลิงก์ไม่สำเร็จ — ไม่พบพิกัดในหน้าเป้าหมาย" อ่านออกว่าเกิดอะไร แต่ไม่บอกทางออก (คนขับ/ธุรการจะติดตรงนี้)
**แก้ (ข้อความเดียว):**
> "ลิงก์นี้เป็นลิงก์ชื่อร้าน ไม่มีพิกัดอยู่ในตัว — เปิดลิงก์ใน Google Maps แล้วกดค้างที่หมุด จะมีตัวเลขพิกัดขึ้นมา คัดลอกมาวางในช่องนี้ได้เลย"

### 🟢 F4 — ลิงก์ "เปิดแผนที่" ที่ `/jobs-nearby` เป็นไอคอนล้วน ไม่มีข้อความ
`MapsLink` ถูกเรียกโดยไม่ส่ง `label` → เหลือแค่ไอคอน (มี `title` ช่วยไว้บ้าง) — WCAG 2.1 §2.4.4 แนะนำให้ลิงก์มีข้อความอ่านได้ และเป้ากดเล็กกว่า 44×44 px ตาม Apple HIG
**แก้:** ส่ง `label="เปิดแผนที่"` เหมือนที่หน้าอื่นทำ

---

## สรุป

**6/6 เคสที่สั่งไว้ผ่านหมด · SSRF 8/8 · auth guard ผ่าน · ไม่มีเคสไหนค้างเงียบ**
ฟีเจอร์นี้ **พร้อม push** ✅ — F1 แนะนำให้แก้ก่อน push (แก้บรรทัดเดียว ~1 นาที) เพราะเปลี่ยนข้อความ error ที่ชวนเข้าใจผิดให้ตรงความจริง ส่วน F2–F4 ตามได้ทีหลัง

**ยังไม่ได้ทดสอบ (บอกตรงๆ):**
- ไม่ได้กด "บันทึกตำแหน่ง" จนเขียนลง production จริงในรอบนี้ (เลี่ยงการทิ้งข้อมูลขยะ — round-trip เคยยืนยันแล้วใน QA รอบ 2)
- ไม่ได้ทดสอบ mobile viewport 375px ของฟีเจอร์นี้ (บั๊ก Sidebar ระดับแอปยังค้างจาก 28/08 ทำให้วัดไม่ได้จริง)
- ลิงก์ย่อจาก iOS Google Maps (ทดสอบเฉพาะลิงก์ที่ Dev1 ส่งมา + ที่ดิฉันหาเองจากเว็บ)

**ภาพหลักฐาน:** `qa-shots/r3/` — `r3-ui-1…11-*.png` (ทุกภาพมาจากเว็บจริง + ล็อกอินจริง)

---

## ✅ ตรวจซ้ำรอบสุดท้าย (1 ก.ย. 2026, หลัง session ขาด) — ยืนยันด้วยมือตัวเองอีกรอบ

รันซ้ำบน dev server + ล็อกอินจริงเดิม เพื่อยืนยันผลก่อนส่ง (ไม่เชื่อ log เก่าอย่างเดียว):

| เคส | ผลที่เห็นบนหน้าเว็บ (ยืนยันซ้ำ) |
|---|---|
| 4 ลิงก์นำทาง `iU2UUamf5qFk24sx6` | `13.723162, 100.739220` + ลิงก์ "เปิดแผนที่" — ตรงกับที่ Dev1 ระบุทุกหลัก |
| 5 ร้าน ตปท. `4Nh2nJoUi7kdZidCA` | ข้อความแดง `แปลงลิงก์ไม่สำเร็จ — ไม่พบพิกัดในหน้าเป้าหมาย` |
| 6 ร้านไทย `qWVEfMYfyYT1hyvR8` | ข้อความแดงเดียวกัน |

ยิง API ซ้ำเองอีก 4 ครั้งสำหรับเคส 4 → `200 {"lat":13.723162,"lng":100.7392196}` **เหมือนกันทั้ง 4 ครั้ง** (เสถียร)
เคส 5/6 ซ้ำอย่างละ 2-3 ครั้ง → `422` ข้อความเดิมทุกครั้ง · auth guard ยิงซ้ำ → `401` · SSRF ยิงซ้ำ 8 แบบ → `400` ทั้งหมด

**ภาพหลักฐานฉบับยืนยัน (ใช้ภาพชุดนี้เป็นหลัก):**
`qa-shots/r3/VERIFY-case4-resolved.png` · `VERIFY-case5-error.png` · `VERIFY-case6-error.png`

> ⚠️ หมายเหตุความโปร่งใส: ภาพชุดเก่าบางไฟล์ **ชื่อไม่ตรงกับเนื้อภาพ** (เช่น `r3-ui-case4-done.png` เป็นภาพช่องว่าง, `r3-ui-case5-notfound.png` เป็นภาพสปินเนอร์ของลิงก์อีกอัน) เพราะจังหวะ capture คลาดกับ state ของหน้า — จึงถ่ายชุด `VERIFY-*.png` ใหม่แทน ผลตัวหนังสือใน `results-ui.txt` / `results-api.txt` ตรงกับการตรวจซ้ำรอบนี้ทุกข้อ

### 🟡 F5 (เพิ่ม) — parser ไม่รู้จักรูปแบบ `!1d<lng>!2d<lat>` ของลิงก์นำทาง
ตอน resolve เคส 4 ผ่าน client ธรรมดา (node fetch เปล่า) Google ตอบ URL อีกแบบที่มีพิกัดจุดหมายอยู่ในรูป `!1d100.7392196!2d13.723162` ซึ่ง `parseGoogleMapsLink()` **ไม่มีกฎรองรับ** จึงตกไปจับ `@13.6860887,100.731166` ที่เป็น**จุดกึ่งกลางแผนที่** — ห่างจากจุดหมายจริง **~4.2 กม.** และไม่มีอะไรเตือน
ผ่านแอปจริงตอนนี้ยังได้ค่าถูก (ยืนยัน 4/4 ครั้ง) เพราะ Google ตอบ URL คนละแบบให้ runtime ของ Next แต่ถ้า Google สลับรูปแบบเมื่อไหร่ ระบบจะ**บันทึกพิกัดผิดแบบเงียบๆ** แทนที่จะ error
**แก้:** เพิ่มกฎ `!1d(lng)!2d(lat)` และ `destination=`/`daddr=` ให้ชนะ `@` (ซึ่งเป็นแค่จุดกึ่งกลางแผนที่ ไม่ใช่หมุด) — ควรทำคู่กับ F1

---

## เก็บกวาดเครื่อง (ยืนยันด้วย netstat ซ้ำหลังสั่งปิด)

รอบก่อนหน้าดิฉันเขียนว่าปิดโปรเซสแล้วทั้งที่ยังค้างอยู่จริง — ผิดที่ตัวเอง แก้แล้วรอบนี้และยืนยันซ้ำ:

| โปรเซส | PID | ผล |
|---|---|---|
| `next dev` (:3000) | 27124 | ปิดแล้ว ✅ |
| headless Chrome (:9422) | 34856 + child 8424 | ปิดแล้ว ✅ |
| headless Chrome (:9433) | 2388 + child 36004 | ปิดแล้ว ✅ |

ตรวจซ้ำหลังปิด: `netstat -ano | grep LISTENING | grep -E ":3000|:9422|:9433"` → **ไม่เหลือรายการใด**
Chrome ของเจ้าของ 49 process **ไม่ถูกแตะ** (ปิดเฉพาะ PID ที่ดิฉัน spawn เอง ตามสกิล safe-process-cleanup)
ไม่ได้แก้ไฟล์โค้ดของโปรเจกต์ · ไม่ได้เขียนข้อมูลค้างไว้ใน production (ไม่ได้กดบันทึกตำแหน่งจริงในรอบนี้)
