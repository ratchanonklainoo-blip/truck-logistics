# QA รอบ 2 — ฟีเจอร์ "งานใกล้รถ" (`/jobs-nearby`)

**ผู้ตรวจ:** Fern (QA Consultant) · **Dev:** Dev1 · **วันที่:** 31 สิงหาคม 2026
**สรุป:** ✅ **พร้อม push** — แต่มี 1 ช่องโหว่ที่ควรปิดตามไปด้วย (ไม่บล็อก)

---

# วิธีทดสอบรอบนี้ — ต่างจากรอบแรกอย่างไร

| | รอบ 1 (30/08) | **รอบ 2 (31/08)** |
|---|---|---|
| ล็อกอิน | ❌ ไม่ได้ล็อกอิน | ✅ **ล็อกอินจริงผ่านฟอร์ม** ด้วย test user |
| Data layer | ❌ stub ที่เขียนเอง | ✅ **Supabase production จริง** |
| middleware | ลบออก | ✅ **เปิดใช้จริง** (ยืนยัน: เข้า `/jobs-nearby` ไม่ล็อกอิน → 307 → `/login`) |
| ข้อมูล | แต่งเองใน memory | ✅ **เขียนลง production จริง แล้วลบทิ้ง** |

ผลรอบนี้จึงเป็น 🟢 **[DB จริง]** เกือบทั้งหมด ไม่ใช่ 🔵 [stub] แบบรอบแรก

**ระดับหลักฐาน:** 🟢 [DB จริง] · 🟡 [โค้ด] · 🔵 [stub] · ⚪ [อนุมาน]

---

## ตารางสรุป — เทียบกับที่ค้างไว้รอบแรก

| จุดทดสอบ | รอบ 1 | **รอบ 2** | หลักฐาน |
|---|---|---|---|
| migration 014 บน production | 🔴 BLOCKER | ✅ **ผ่าน** | 🟢 [DB จริง] |
| BUG1 — งานไม่มีพิกัดปลายทางคว้าแถบเขียว | 🐛 พบ | ✅ **แก้แล้ว ยืนยัน** | 🟢 [DB จริง] |
| BUG2 — validate lat/lng (UI) | 🐛 พบ | ✅ **แก้แล้ว ยืนยัน** | 🟢 [DB จริง] |
| BUG2 — validate lat/lng (API route) | — | ✅ **บล็อกจริง** | 🟢 [DB จริง] |
| **BUG2 — bypass ยิง REST ตรง** | — | ❌ **ยังทะลุได้** | 🟢 [DB จริง] |
| label ช่องพิกัดที่ `/jobs` | 🟠 ไม่มี | ✅ **มีแล้ว + htmlFor** | 🟡 [โค้ด] |
| ตัวกรองรัศมี 20/50/100/ไม่จำกัด | 🔵 stub | ✅ **ผ่านบนของจริง** | 🟢 [DB จริง] |
| สลับเรียง "ใกล้ที่สุด" | 🔵 stub | ✅ **ผ่านบนของจริง** | 🟢 [DB จริง] |
| เลือกพิกัดล่าสุด | 🔵 stub | ✅ **ผ่านบนของจริง** | 🟢 [DB จริง] |
| กรองงานที่จัดรถแล้วออก | 🔵 stub | ✅ **ผ่านบนของจริง** | 🟢 [DB จริง] |
| แถบเตือนสีเหลือง | 🔵 stub | ✅ **ผ่านบนของจริง** | 🟢 [DB จริง] |
| round-trip บันทึกตำแหน่ง→เห็นในหน้า | 🔵 stub | ✅ **ผ่านบนของจริง** | 🟢 [DB จริง] |
| console สะอาด | 🔵 stub | ✅ **สะอาด 0 entry** | 🟢 [DB จริง] |
| `NUMERIC` number หรือ string | ⚪ อนุมาน | ✅ **เป็น number ยืนยันแล้ว** | 🟢 [DB จริง] |
| mobile 375px | ❌ FAIL | ❌ **ยัง FAIL** (บั๊กระดับแอป) | 🟢 [DB จริง] |
| role gating ที่ UI | ❌ ไม่มี | ❌ ยังไม่มี (นอกขอบเขตงานนี้) | 🟡 [โค้ด] |

---

## ✅ ยืนยัน 3 จุดที่ Dev1 เพิ่งแก้

### 1. migration 014 ขึ้น production แล้ว  🟢 [DB จริง]
```
GET /rest/v1/truck_locations?select=id&limit=1   → 200 []
GET /rest/v1/jobs?select=id,origin_lat,origin_lng,
    destination_lat,destination_lng&limit=1      → 200 []
```
รอบแรกได้ `404 PGRST205` และ `400 42703` — ตอนนี้ตอบ 200 ทั้งคู่
โหลดหน้า `/jobs-nearby` ด้วย login จริง **ไม่พัง ไม่มี console error เลยแม้แต่รายการเดียว** ✅

### 2. BUG1 แก้แล้ว — งานไม่มีพิกัดปลายทางไม่คว้าแถบเขียวอีก  🟢 [DB จริง]

**โค้ดที่แก้** (`jobs-nearby/page.tsx:85-86`) ตรงตามที่เสนอ:
```ts
const totalKm   = legKm !== null ? pickupKm + legKm : null;
const bahtPerKm = (totalKm !== null && totalKm > 0) ? job.selling_price / totalKm : null;
```

**ผลจริงบนหน้าเว็บ** — คนขับ **เอก** (@ 19.9105, 99.8406):

| งาน | ไปรับ | วิ่งงาน | บาท/กม. | แถบเขียว |
|---|---|---|---|---|
| `__QA_2` เชียงราย→พะเยา | 0.0 กม. | 83.0 กม. | **144.6** | 🟢 **คุ้มเที่ยวสุด** |
| `__QA_1` แม่จัน→เชียงใหม่ | 26.1 กม. | 176.4 กม. | 88.9 | |
| `__QA_3` ลพบุรี→กรุงเทพ | 574.8 กม. | 117.1 กม. | 36.1 | |
| **`__QA_4_NODEST` แม่สาย→ฝาง** | 57.9 กม. | **ไม่มีพิกัดปลายทาง** | **—** | ❌ **ไม่ติดแล้ว** |

รอบแรกงานนี้ได้ **155.4** แล้วคว้าแถบเขียวไป — ตอนนี้แสดง `—` และถูกตัดออกจากการแข่งขันถูกต้อง
ตรวจซ้ำกับคนขับ **จง** ก็ได้ผลเดียวกัน (`—`, ไม่ติดแถบ) ✅

ตัวเลขที่เหลือตรงกับที่คำนวณมือด้วย Python แยกชุดทุกหลัก (144.62 / 88.86 / 36.13)

### 3. BUG2 validate lat/lng  🟢 [DB จริง]

**ชั้น UI — `/drivers`:**
- input มี `min="-90" max="90"` และ `min="-180" max="180"` แล้ว ✅
- กรอก **lat=999 lng=888** กด "บันทึกตำแหน่ง" → ถูกบล็อก พร้อมข้อความ:
  > `พิกัดนอกช่วงที่เป็นไปได้ (lat ต้องอยู่ระหว่าง -90 ถึง 90, lng ระหว่าง -180 ถึง 180)` ✅
- กรอกค่าถูกต้อง (20.1450 / 99.8580) → บันทึกผ่าน ไม่มี alert ✅

**ชั้น API route** (ยิงผ่าน cookie ที่ล็อกอินจริง):

| คำขอ | ผล |
|---|---|
| `POST /api/jobs` `origin_lat: 999` | **400** `{"error":"invalid latitude (must be -90..90)"}` ✅ |
| `POST /api/jobs` `origin_lng: 888` | **400** `{"error":"invalid longitude (must be -180..180)"}` ✅ |
| `POST /api/jobs` พิกัดถูกต้อง | **201** สร้างสำเร็จ ✅ |
| `PATCH /api/jobs/[id]` `origin_lat: 999` | **400** `{"error":"invalid origin_lat (must be -90..90)"}` ✅ |

**label ช่องพิกัดที่ `/jobs`** — แก้แล้ว มี `<label htmlFor>` ผูกกับ input จริง:
`พิกัดต้นทาง (lat, lng)` และ `พิกัดปลายทาง (lat, lng)` ✅

---

## ❌ ช่องโหว่ที่ยังเหลือ — validation ทะลุได้ถ้ายิง REST ตรง  🟢 [DB จริง]

**นี่คือจุดเดียวที่ยังไม่ปิด** และเป็นสิ่งที่ CEO สั่งให้ทดสอบโดยเฉพาะ

`truck_locations` **ไม่มี API route เลย** — หน้า `/drivers` เรียก `supabase.from('truck_locations').insert()` จาก client ตรงๆ (`drivers/page.tsx:194`)
ดังนั้น validation ที่ Dev1 เพิ่ง**เพิ่มอยู่ใน JavaScript ฝั่ง browser เท่านั้น**

**ทดสอบจริงบน production** (ใช้ token ของ test user):
```
POST /rest/v1/truck_locations
  {"driver_id":"<เอก>","lat":999,"lng":888,"source":"manual"}
→ 201 Created
  {"lat":999.000000,"lng":888.000000,...}      ← ผ่านฉลุย
```
ฝั่ง `jobs` ก็เหมือนกัน — ยิง `POST /rest/v1/jobs` ตรงด้วย `origin_lat:999` ก็ **201 สำเร็จ** (ข้าม API route ที่ validate ไว้)

> ✅ แถวทดสอบทั้งหมดถูกลบออกจาก production แล้ว ยืนยันเหลือ 0

**ทำไมยังทะลุ:** migration 014 **ไม่มี CHECK constraint** บน `lat`/`lng`
```sql
lat  NUMERIC(9,6) NOT NULL,     -- ไม่มี CHECK
lng  NUMERIC(9,6) NOT NULL,     -- ไม่มี CHECK
```

**ความเสี่ยงจริงแค่ไหน:** 🟡 ปานกลาง–ต่ำ
- ต้องมี token ที่ล็อกอินแล้วถึงยิงได้ (anon ยิงไม่ได้ — ยืนยันแล้ว)
- ตอนนี้มีแต่ออฟฟิศที่ล็อกอิน ยังไม่เปิดให้คนขับ
- **แต่** ถ้าอนาคตต่อ GPSIAM อัตโนมัติ หรือให้คนขับล็อกอิน ค่าเพี้ยนจะเข้าได้โดยไม่มีอะไรกัน แล้วทำให้ บาท/กม. ของคนขับคนนั้นผิดทั้งหมดแบบเงียบๆ

**แก้ (แนะนำ) — เพิ่ม migration 015 ปิดที่ชั้น DB ให้จบ:**
```sql
ALTER TABLE truck_locations
  ADD CONSTRAINT truck_locations_lat_range CHECK (lat BETWEEN -90 AND 90),
  ADD CONSTRAINT truck_locations_lng_range CHECK (lng BETWEEN -180 AND 180);

ALTER TABLE jobs
  ADD CONSTRAINT jobs_origin_lat_range      CHECK (origin_lat      IS NULL OR origin_lat      BETWEEN -90  AND 90),
  ADD CONSTRAINT jobs_origin_lng_range      CHECK (origin_lng      IS NULL OR origin_lng      BETWEEN -180 AND 180),
  ADD CONSTRAINT jobs_destination_lat_range CHECK (destination_lat IS NULL OR destination_lat BETWEEN -90  AND 90),
  ADD CONSTRAINT jobs_destination_lng_range CHECK (destination_lng IS NULL OR destination_lng BETWEEN -180 AND 180);
```
ปิดที่ชั้น DB ครอบทุกทางเข้า (UI, API, REST ตรง, script) ทีเดียวจบ — และเป็นชั้นที่ bypass ไม่ได้

---

## ✅ ข้อที่รอบแรกติดป้าย 🔵 stub — ตอนนี้ยืนยันบนของจริงแล้วทุกข้อ

ทดสอบด้วยข้อมูลที่เขียนลง production จริง: งาน 6 งาน + ตำแหน่งรถ 3 แถว (ลบหมดแล้ว)

### ตัวกรองรัศมี 🟢 — ตรงเป๊ะทุกค่า
คนขับ **เอก** @ เชียงราย:

| รัศมี | ผลจริง | คาดหวัง | |
|---|---|---|---|
| 20 กม. | `__QA_2` | QA2 | ✅ |
| 50 กม. | `__QA_2`, `__QA_1` | QA2, QA1 | ✅ |
| 100 กม. | `__QA_2`, `__QA_1`, `__QA_4` | QA2, QA1, QA4 | ✅ |
| ไม่จำกัด | ครบ 4 งาน | ครบ 4 | ✅ |

คนขับ **จง** @ แม่จัน ได้ผลสอดคล้องกับระยะจริงของตัวเองแยกต่างหาก (20 กม. → `__QA_1` เพราะต้นทาง QA1 คือแม่จันพอดี) ✅

### สลับเรียง "ใกล้ที่สุด" 🟢
- **เอก:** `__QA_2`(0.0) → `__QA_1`(26.1) → `__QA_4`(57.9) → `__QA_3`(574.8) ✅
- **จง:** `__QA_1`(0.0) → `__QA_2`(26.1) → `__QA_4`(31.8) → `__QA_3`(600.3) ✅

ลำดับเปลี่ยนตามระยะทางจริง ไม่ใช่ตามบาท/กม. และแถบเขียวยังคงอยู่ที่งานคุ้มสุดเหมือนเดิม (ตามที่ออกแบบ)

### เลือกพิกัดล่าสุด 🟢
ใส่ให้ **เอก** 2 แถวจริงใน DB: `18.0, 98.0` (29 ส.ค.) และ `19.9105, 99.8406` (31 ส.ค.)
หน้าเว็บแสดง **`19.9105, 99.8406`** = แถวใหม่กว่า ✅ ตรรกะ `order recorded_at desc` + first-per-driver ทำงานถูกบน PostgREST จริง

### กรองงานที่จัดรถแล้วออก 🟢
สร้าง `__QA_6_ASSIGNED` (status `assigned`, ผูกกับเอก) → **ไม่โผล่ในรายการของคนขับคนใดเลย** ✅

### แถบเตือนสีเหลือง 🟢
สร้าง `__QA_5_NOCOORD` (ไม่มีพิกัดต้นทาง) → ขึ้นแถบถูกต้อง:
> "มีงานที่ยังไม่จัดรถอีก **1 งาน**ที่ยังไม่ได้กรอกพิกัดต้นทาง — จะไม่แสดงในหน้านี้จนกว่าจะกรอกพิกัดที่หน้า "งานเข้า"" ✅

### round-trip บันทึกตำแหน่ง → เห็นในหน้า nearby 🟢
`/drivers` กรอก 20.1450 / 99.8580 → กด "บันทึกตำแหน่ง" → เห็น "บันทึกล่าสุด: 20.145, 99.858"
→ คลิกเมนู "งานใกล้รถ" → คนขับคนนั้นแสดง `20.1450, 99.8580` พร้อมรายการงานทันที ✅
(เขียนลง DB จริงแล้วอ่านกลับมาจริง ไม่ใช่ state ใน memory)

### console สะอาด 🟢
รอบเดินหน้าปกติ: **0 error, 0 warning, 0 failed request** — ไม่มีแม้แต่ `favicon.ico` 404 (หายไปเพราะ Next.js dev serve ให้เอง)
มี 400 ปรากฏ 3 ครั้งเฉพาะตอนที่ดิฉัน**ตั้งใจยิงทดสอบ validation** ซึ่งคือพฤติกรรมที่ถูกต้อง

### `NUMERIC` เป็น number ไม่ใช่ string 🟢 — ปิดประเด็น ⚪ ที่ค้างจากรอบแรก
PostgREST คืนค่าเป็น JSON number จริง เช่น `"lat":999.000000`, `"selling_price":6000.00`
`loc.lat.toFixed(4)` (บรรทัด 180) เรนเดอร์ `20.1450` ได้ปกติ **ไม่มี TypeError** และ `haversineKm` คำนวณถูกต้อง ✅
ความเสี่ยงที่รอบแรกยกไว้ **ไม่เกิดขึ้นจริง**

---

## ❌ mobile 375px — ยัง FAIL (บั๊กระดับแอป ไม่ใช่ของ Dev1)  🟢 [DB จริง]

| ค่าวัดจริง @375px | |
|---|---|
| `<main>` เริ่มที่ x | 256px |
| `<main>` กว้างที่เหลือ | **119px** |
| `overflow-x` | `hidden` |
| `scrollWidth` | 375px (เลื่อนไปหาไม่ได้) |

ตัวควบคุมที่หลุดนอกจอ: select รัศมี (x=401) · select เรียง (x=559) · ปุ่มรีเฟรช (x=662) — **ทั้ง 3 ตัวคือหัวใจของฟีเจอร์**

เป็นบั๊ก `Sidebar.tsx` + `(main)/layout.tsx` ที่รายงานเป็น CRITICAL #1 ตั้งแต่ 28/08 **ยังไม่ถูกแก้**
**ไม่ควรบล็อกการ push ฟีเจอร์นี้** เพราะ (ก) ไม่ใช่ regression จากงานนี้ (ข) ทั้งแอปเป็นเหมือนกันหมด (ค) หน้านี้เขียน responsive ของตัวเองไว้ดีแล้ว พอแก้ sidebar เสร็จจะใช้ได้ทันที
แต่ควรเปิดเป็นงานแยกให้ Dev1 ทำต่อ

---

## สรุป: พร้อม push ✅

**3 จุดที่สั่งให้แก้ — แก้ครบและยืนยันบน production จริงแล้วทั้งหมด**
ตรรกะหลักทุกข้อที่รอบแรกยืนยันไม่ได้ ตอนนี้ผ่านบนข้อมูลจริงหมดแล้ว และ console สะอาดสนิท

**push ได้เลย** โดยแนะนำให้ทำ 2 อย่างตามไปด้วย:

| ลำดับ | รายการ | ระดับ |
|---|---|---|
| 1 | เพิ่ม **migration 015** ใส่ CHECK constraint lat/lng ทั้ง `truck_locations` และ `jobs` | 🟠 ควรทำเร็ว — ปิดช่องโหว่ที่เหลืออยู่จุดเดียว |
| 2 | แก้ sidebar ให้ responsive (งานแยก กระทบทั้งแอป) | 🟡 ตามได้ |

ข้อ 1 ใช้เวลาไม่ถึง 10 นาที และเป็นชั้นเดียวที่ bypass ไม่ได้ — ถ้าทำก่อน push ได้ยิ่งดี แต่ไม่ถือว่าบล็อก เพราะตอนนี้ทางเข้าที่ผู้ใช้จริงใช้ (UI + API route) ปิดครบแล้ว

---

## บันทึกความโปร่งใส

- **ล็อกอินจริง** ด้วย test user ที่ Dev1 สร้างให้ (`qa.fern.disposable.*@bagidea-qa-test.com`) — ยืนยันเข้าถึง `/dashboard` ได้จริง
- **middleware ทำงานจริง** — เข้า `/jobs-nearby` โดยไม่ล็อกอิน → `307` → `/login`
- **เขียน/ลบข้อมูลบน production จริง:** สร้างงานทดสอบ 6 งาน (`job_number` ขึ้นต้น `__QA_`), งานจาก API 1 งาน, ตำแหน่งรถ 5 แถว, และแถว bypass 2 แถว
  **ลบออกหมดแล้ว ยืนยัน:** `jobs __QA_*` = 0 · `jobs origin __QA_*` = 0 · `truck_locations` = 0 (ตารางนี้ว่างเปล่าก่อนเริ่ม QA)
  **ข้อมูลจริงของบริษัทไม่ถูกแตะ:** งานเดิม `JOB-20260505-0001` (ลำปาง→เชียงราย) ยังอยู่ครบ · drivers 3 คนครบ
- **ไม่ได้แก้โค้ดโปรเจกต์:** `git status` เท่าเดิม (9 ไฟล์ที่ Dev1 แก้ + ไฟล์ใหม่)
- **process:** dev server **PID 30396** ปิดแล้ว · headless chrome ปิด PID ตัวเองตอนจบ (กรองซ้ำด้วย debug port 9401/9407) · **Chrome ของเจ้าของ 21 process ไม่ถูกแตะ**
- **token ที่ใช้ทดสอบลบทิ้งแล้ว**

### สิ่งที่ยัง **ไม่ได้** ทดสอบ (บอกตรงๆ)
- ⛔ พฤติกรรมแยกตาม **role** (`bank`/`mother`/`admin`/`driver`) — test user มี role เดียว ยังไม่ได้ไล่ทีละ role
- ⛔ **RLS ต่อ role** ว่าปล่อย/บล็อกต่างกันไหม (ทั้งแอปเป็น `TO authenticated USING(true)` เหมือนกันหมดอยู่แล้ว)
- ⛔ พฤติกรรมเมื่อ `truck_locations` มีข้อมูลจำนวนมาก (query ไม่มี `.limit()` — จะช้าลงถ้าต่อ GPSIAM อัตโนมัติ)

### ภาพหลักฐาน (ทุกภาพมาจาก production จริง + ล็อกอินจริง)
| ไฟล์ | คำอธิบาย |
|---|---|
| `qa-shots/round2/r2-nearby-default.png` | หน้า /jobs-nearby ข้อมูลจริง — `__QA_4_NODEST` แสดง `—` ไม่ติดแถบเขียว |
| `qa-shots/round2/r2-nearby-sortdist.png` | สลับเรียง "ใกล้ที่สุด" |
| `qa-shots/round2/r2-drivers-999-blocked.png` | กรอก 999/888 ถูกบล็อก |
| `qa-shots/round2/r2-roundtrip.png` | หลังบันทึกตำแหน่งแล้วกลับมาหน้า nearby |
| `qa-shots/round2/r2-mobile-375.png` | mobile 375 — ตัวควบคุมนอกจอ |
| `qa-shots/round2/r2-after-login.png` | หลังล็อกอินจริง เข้า /dashboard |
