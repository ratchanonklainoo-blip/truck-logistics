# QA Report — ฟีเจอร์ "งานใกล้รถ" (`/jobs-nearby`)

**ผู้ตรวจ:** Fern (QA Consultant) · **Dev:** Dev1
**เขียนครั้งแรก:** 30 ส.ค. 2026 · **แก้ไขฉบับนี้:** 30 ส.ค. 2026 (หลังถูก review ทักเรื่องวิธีทดสอบ)
**สรุป:** ⛔ **ยังไม่พร้อม push**

---

# ⚠️ อ่านตรงนี้ก่อน — ข้อจำกัดของการทดสอบครั้งนี้

**ดิฉันไม่ได้ล็อกอินเข้าแอปจริง และไม่ได้แตะฐานข้อมูลจริงแม้แต่ครั้งเดียวในการทดสอบ UI**

- ไม่มี credential สำหรับล็อกอิน · เครื่องไม่มี Docker (รัน Supabase local ไม่ได้)
- ดิฉันเลือก **ไม่** สร้าง test user บน production auth เพราะไม่มี service-role key ไว้ลบทิ้ง
- จึงก๊อปโปรเจกต์ไป `%TEMP%` แล้ว **เขียน stub ของ `lib/supabase/{client,server}.ts` ขึ้นมาเอง** พร้อม **ข้อมูลทดสอบที่ดิฉันแต่งเองทั้งหมด** (คนขับ 3 คน, งาน 6 งาน, พิกัดทุกจุด)
- React component / CSS / routing / การคำนวณ = ของจริง
- **data layer = ของปลอมที่ดิฉันเขียนเอง 100%**

**ผลที่ตามมา — ต้องเข้าใจให้ตรงกัน:**

| สิ่งที่พิสูจน์ได้จริง | สิ่งที่ **ยังพิสูจน์ไม่ได้** |
|---|---|
| สูตร haversine ถูก | query จริงคืนข้อมูลตรงตามที่หน้าคาดหวังไหม |
| ตรรกะเรียง/กรอง/เลือก best ในโค้ด React ถูก | PostgREST คืน `NUMERIC` เป็น number หรือ string |
| layout / responsive / console | RLS ปล่อยแถวที่ควรปล่อยจริงไหม |
| โครงสร้างโค้ด, การมี/ไม่มี label, การมี/ไม่มี validation | การเขียนลง DB จริงสำเร็จหรือ error |

การ "เทียบมือด้วย Python" พิสูจน์แค่ว่า **สูตร haversine ถูก** เท่านั้น — ไม่ได้พิสูจน์ว่า pipeline ทำงาน เพราะพิกัด/ราคา/ระยะทางทั้ง 4 งานดิฉันเป็นคนกำหนดเอง

ที่สำคัญกว่านั้น: หัวข้อ **"เลือกพิกัดล่าสุดถูก" / "กรองงานที่จัดรถแล้วออก" / "แถบเตือนเหลือง"** ล้วนขึ้นกับว่า stub ของดิฉันจำลอง semantics ของ `.in()` / `.is()` / `.order()` ได้ตรงกับ PostgREST จริงไหม — **ซึ่งเป็นสิ่งที่กำลังถูกทดสอบเอง เป็นการวนพิสูจน์ตัวเอง** ผลเหล่านั้นจึงบอกได้แค่ว่า "โค้ด React ทำถูกถ้า DB คืนข้อมูลมาแบบที่ stub คืน"

> **สรุปสั้น:** BLOCKER และ BUG ทั้ง 3 ข้อยืนยันจาก **DB จริง / โค้ดจริง** เชื่อได้
> แต่ช่อง **PASS ทุกช่องที่ติดป้าย 🔵 ยังไม่ใช่หลักฐานว่าฟีเจอร์ทำงานบนของจริง** — ต้องตรวจซ้ำหลังรัน migration

---

## ระดับความน่าเชื่อของหลักฐาน (ใช้กำกับทุกข้อในรายงาน)

| ป้าย | ความหมาย |
|---|---|
| 🟢 **[DB จริง]** | ยิง REST API ไปที่ Supabase production จริง |
| 🟡 **[โค้ด]** | อ่านซอร์สจริง — การมี/ไม่มีของโค้ดพิสูจน์ได้แน่นอน |
| 🔵 **[stub]** | สังเกตในเบราว์เซอร์จริง แต่ข้อมูลมาจาก mock ที่ดิฉันเขียนเอง |
| ⚪ **[อนุมาน]** | คาดการณ์จากเอกสาร/DDL — **ยังไม่ได้ทดสอบ** |

---

## ตารางสรุปผล

| # | จุดทดสอบ | ผล | หลักฐาน |
|---|---|---|---|
| — | **migration 014 บน production** | 🔴 **BLOCKER — ยังไม่รัน** | 🟢 [DB จริง] |
| 1 | `/jobs` กรอกพิกัด origin/destination | ⚠️ PASS มีปัญหา — ช่องพิกัดไม่มี label | 🔵 stub + 🟡 โค้ด |
| 2 | `/drivers` บันทึกตำแหน่ง | ⚠️ PASS มีปัญหา — รับพิกัดนอกโลกได้ | 🔵 stub + 🟡 โค้ด |
| 3 | `/jobs-nearby` แสดงตำแหน่ง + เรียงบาท/กม. + แถบเขียว | ⚠️ PASS มี BUG 1 | 🔵 stub |
| 4 | ตัวกรองรัศมี 20/50/100/ไม่จำกัด | ✅ PASS *(บน stub)* | 🔵 stub |
| 5 | สลับเรียง "ใกล้ที่สุด" | ✅ PASS *(บน stub)* | 🔵 stub |
| 6 | แถบเตือนสีเหลือง | ✅ PASS *(บน stub)* | 🔵 stub |
| + | สูตรคำนวณบาท/กม. | ✅ PASS | 🟡 โค้ด + คำนวณมือ |
| + | console error | ✅ สะอาด *(บน stub)* | 🔵 stub |
| + | role — ชั้น frontend | ❌ ไม่มี gating เลย | 🟡 โค้ด |
| + | role — ชั้น backend (RLS) | ⚠️ ใครที่ล็อกอินก็แก้ได้หมด | 🟡 โค้ด (policy) |
| + | role — พฤติกรรม runtime ต่อ role | ⛔ **ไม่ได้ทดสอบ** (ล็อกอินไม่ได้) | — |
| + | mobile 375px | ❌ FAIL | 🔵 stub *(แต่เป็นบั๊ก CSS ล้วน)* |
| + | `NUMERIC` คืนเป็น number หรือ string | ⛔ **ไม่ได้ทดสอบ** | ⚪ อนุมาน |

---

## 🔴 BLOCKER — migration 014 ยังไม่ถูกรันบน production  🟢 [DB จริง]

```
GET /rest/v1/truck_locations?select=id&limit=1
→ 404  {"code":"PGRST205","message":"Could not find the table 'public.truck_locations' in the schema cache"}

GET /rest/v1/jobs?select=id,origin_lat,origin_lng&limit=1
→ 400  {"code":"42703","message":"column jobs.origin_lat does not exist"}

GET /rest/v1/jobs?select=id&limit=1          (baseline เทียบ)
→ 200  []
```

typecheck/build ผ่านเพราะ TypeScript ไม่รู้จัก schema จริง
ถ้า push ตอนนี้: `/jobs-nearby` พัง, `/drivers` query `truck_locations` พัง, บันทึกงานที่ `/jobs` error

**ต้องรัน migration ก่อน** แล้ว re-probe ยืนยันว่าทั้ง 2 endpoint ตอบ 200
> ดิฉันไม่ได้รัน migration ให้เอง — เป็นการเปลี่ยน schema production ต้องให้ CEO/Dev1 สั่งเอง

---

## 🐛 BUG 1 — งาน "ไม่มีพิกัดปลายทาง" แย่งแถบ "คุ้มเที่ยวสุด"  🟡 [โค้ด] + 🔵 [stub]

**ต้นเหตุพิสูจน์จากโค้ดโดยตรง** — `jobs-nearby/page.tsx:82-86`
```ts
const legKm = (job.destination_lat !== null && job.destination_lng !== null)
  ? haversineKm(...) : null;
const totalKm   = pickupKm + (legKm || 0);        // ← legKm=null กลายเป็น 0
const bahtPerKm = totalKm > 0 ? job.selling_price / totalKm : null;
```
`bahtPerKm` แทบไม่มีทางเป็น `null` (เป็นได้เฉพาะตอน `totalKm === 0`) → งานข้อมูลไม่ครบเข้าไปแข่งใน `bestJobIdByDriver` ด้วยตัวเลขที่พองเกินจริง

**ข้อนี้เป็นบั๊กแน่นอนโดยไม่ต้องพึ่ง stub** — อ่านจากโค้ดก็เห็นว่าคณิตศาสตร์ผิด: หารรายได้ทั้งเที่ยวด้วยแค่ระยะไปรับ

ตัวเลขข้างล่างมาจาก stub (ข้อมูลดิฉันแต่งเอง) ใช้เพื่อ**อธิบายให้เห็นภาพ** ไม่ใช่หลักฐานยืนยัน:

| งาน (ข้อมูลสมมติ) | ไปรับ | วิ่งงาน | บาท | บาท/กม. | |
|---|---|---|---|---|---|
| JOB-004 แม่สาย → ฝาง | 57.9 | **ไม่มีพิกัดปลายทาง** | ฿9,000 | **155.4** | 🟢 คุ้มเที่ยวสุด |
| JOB-002 เชียงราย → พะเยา | 0.0 | 83.0 | ฿12,000 | 144.6 | |

งานถูกกว่าที่ข้อมูลไม่ครบ ชนะงานแพงกว่าที่ข้อมูลครบ — และยิ่งอยู่ใกล้ยิ่งพอง (ทดสอบอีกเคสได้ 282)

**ผลกระทบ:** ออฟฟิศจัดรถตามแถบเขียว → ระบบเชียร์งานที่กรอกข้อมูลไม่ครบ ซึ่งตรงข้ามกับเจตนาของฟีเจอร์

**แก้:**
```ts
const totalKm   = legKm !== null ? pickupKm + legKm : null;
const bahtPerKm = (totalKm && totalKm > 0) ? job.selling_price / totalKm : null;
```
บรรทัด 228 แสดง `—` อยู่แล้ว และ `bestJobIdByDriver` กรอง `bahtPerKm !== null` อยู่แล้ว จึงรองรับได้ทันที

---

## 🐛 BUG 2 — ไม่มีการตรวจช่วงพิกัด  🟡 [โค้ด] + 🔵 [stub]

**หลักฐานจากโค้ด (แน่นอน):** `drivers/page.tsx` → `handleSaveLocation` ตรวจแค่
```ts
if (!entry?.lat || !entry?.lng || Number.isNaN(lat) || Number.isNaN(lng)) { alert(...); return; }
```
ไม่มีการเช็คช่วง และ input วัดได้ `min=null max=null` ทั้ง 2 ช่อง
ฝั่ง API ก็ไม่ตรวจ — `POST /api/jobs` ใช้ `origin_lat ?? null` เฉยๆ และ `PATCH /api/jobs/[id]` ส่งผ่านด้วย `...rest` ไม่ validate อะไรเลย

ค่าที่ถูกต้อง: lat ∈ [-90, 90] · lng ∈ [-180, 180]

**บน stub:** กรอก lat=999 lng=888 → บันทึกผ่าน ไม่มี alert 🔵

> 🔧 **แก้คำที่เขียนเกินจริงในฉบับแรก:** ฉบับแรกดิฉันเขียนว่า *"ค่าผิดนี้ลงฐานข้อมูลจริงได้ ไม่ถูก DB ปัดตก"* เหมือนได้ทดสอบมาแล้ว — **ไม่จริง ดิฉันไม่ได้ทดสอบ** เพราะตาราง `truck_locations` ยังไม่มีบน DB
> สิ่งที่พูดได้จริงคือ ⚪ **[อนุมานจาก DDL]**: `NUMERIC(9,6)` = precision 9, scale 6 → เหลือ 3 หลักหน้าจุด → เก็บได้ถึง 999.999999 ดังนั้น **ในทางทฤษฎี** 999/888 น่าจะผ่าน DB ไปได้ แต่ต้องยืนยันจริงหลังรัน migration

**แก้:** เช็คช่วงทั้งใน `handleSaveLocation` และใน API + ใส่ `min`/`max` ที่ input ทั้ง 6 ช่อง (2 ที่ /drivers, 4 ที่ /jobs)

---

## 🟠 ISSUE 3 — ช่องพิกัดที่ `/jobs` ไม่มี label  🟡 [โค้ด] + 🔵 [stub]

4 ช่องพิกัดใช้ **placeholder อย่างเดียว** ไม่มี `<label>` (ยืนยันจาก diff ของ `jobs/page.tsx` โดยตรง — ไม่มี `label-text` ในบล็อกนั้น)
ฟิลด์อื่นทุกตัวมี label ครบ: `วันที่`, `แหล่งงาน`, `ลูกค้า`, `ต้นทาง *`, `ปลายทาง *`, `สินค้า`, `น้ำหนัก (กก.)`, `ราคาค่าขนส่ง (บาท) *`, `รูปแบบชำระ`, `จัดรถ`, `หมายเหตุ`

พอกรอกค่าแล้ว placeholder หาย เหลือกล่องตัวเลข 4 กล่องลอยๆ → แยกไม่ออกว่าอันไหน lat/lng/ต้นทาง/ปลายทาง
สลับ lat↔lng = ได้พิกัดคนละที่ในโลก และไม่มีอะไรฟ้อง (เพราะ BUG 2) · ผิด WCAG 2.1 §1.3.1 / §3.3.2

**แก้:** ใส่ `<label className="label-text">พิกัดต้นทาง (lat, lng)</label>` และ `พิกัดปลายทาง (lat, lng)` ครอบแต่ละคู่ + ผูก `id`/`htmlFor`

---

## ❌ ISSUE 4 — mobile 375px: ตัวควบคุมของฟีเจอร์อยู่นอกจอ  🔵 [stub] แต่เป็น CSS ล้วน

| ค่าวัดได้ @375px | |
|---|---|
| `<main>` เริ่มที่ x | 256px |
| `<main>` กว้างที่เหลือ | **119px** |
| `<main>` overflow-x | `hidden` → เลื่อนไปหาไม่ได้ |

ตัวควบคุมที่หลุดนอกจอ: select รัศมี (x=401) · select เรียง (x=559) · ปุ่มรีเฟรช (x=662) — **ทั้ง 3 ตัวคือหัวใจของฟีเจอร์**

> ข้อนี้แม้วัดบน stub แต่ **ไม่ขึ้นกับข้อมูล** เลย — เป็นผลจาก `Sidebar.tsx:91` (`fixed w-64` ไม่มี responsive class) + `(main)/layout.tsx:24` (`ml-64 overflow-x-hidden`) ซึ่งอ่านจากโค้ดยืนยันได้ 🟡 จึงเชื่อได้ว่าเกิดบนของจริงเหมือนกัน

เป็นบั๊ก sidebar ระดับแอปที่ดิฉันรายงานเป็น CRITICAL #1 เมื่อ 28/08 และยังไม่ถูกแก้ — **ไม่ใช่ความผิดของ Dev1** ตัวหน้า `/jobs-nearby` เองเขียน responsive ไว้ดี (`flex-wrap`, `min-w-[200px]`) พอแก้ sidebar เสร็จน่าจะใช้ได้เลย

---

## 🔐 เรื่อง role — ตรวจ 2 ชั้นตามที่ CEO ย้ำเสมอ

### ชั้น 1: Frontend  🟡 [โค้ด] — ❌ ไม่มี gating เลย
`Sidebar.tsx` ใช้ `NAV_ITEMS` + `PHASE_AVAILABLE` แบบ static ไม่อ้างถึง role ที่ใดเลย (grep ทั้งไฟล์ = 0 hit)
`jobs-nearby/page.tsx` ก็ไม่เช็ค role
→ **ทุกคนที่ล็อกอินเห็นเมนูครบทุกอัน** รวม "งานใกล้รถ" ที่เพิ่มใหม่
หน้านี้เปิดเผย **ตำแหน่งรถของคนขับทุกคน + ราคาขายของงานที่ยังไม่จัดรถทั้งหมด**

### ชั้น 2: Backend / RLS  🟡 [โค้ด — อ่าน policy จริง]

`truck_locations` (migration 014):
```sql
CREATE POLICY "truck_locations_all" ON truck_locations
  FOR ALL USING (auth.role() = 'authenticated');
```
→ **ผู้ใช้ที่ล็อกอินคนใดก็ได้ อ่าน/แก้/ลบตำแหน่งของคนขับคนไหนก็ได้** ไม่มีการผูกกับเจ้าของแถว

**แต่ต้องให้ความเป็นธรรมกับ Dev1:** นี่คือ**รูปแบบเดียวกับทั้งแอป** ทุกตารางเป็นแบบนี้หมด —
`jobs`, `drivers`, `customers`, `trips`, `expenses`, `alerts`, `fuel_events`, `advance_requests` ล้วนเป็น `FOR ALL TO authenticated USING (true) WITH CHECK (true)`
014 จึง**ไม่ได้แย่กว่ามาตรฐานเดิมของโปรเจกต์** (ต่างแค่สไตล์ — 014 ใช้ `USING (auth.role()=...)` ตามแบบ migration 008/009 แทน `TO authenticated` แบบ 002/003)
ฉบับแรกดิฉันเขียนข้อนี้เหมือนเป็นปัญหาใหม่ของฟีเจอร์นี้ ซึ่งไม่แฟร์

### สิ่งที่ **ยังไม่ได้ทดสอบ** ⛔
- ไม่ได้ล็อกอินด้วย role ใดเลย (bank / mother / admin / driver) → **ไม่ยืนยันพฤติกรรม runtime**
- ไม่ได้ทดสอบว่า RLS ปล่อย/บล็อกจริงตาม policy ที่เขียนไว้

### สิ่งที่ทดสอบได้จริงและผ่าน 🟢 [DB จริง]
anon (ไม่ล็อกอิน) `SELECT` ทุกตารางหลัก (`jobs`, `drivers`, `trips`, `customers`, `fuel_events`, `advance_requests`, `expenses`, `app_settings`) → **http 200 แต่ได้ `[]` ทุกตาราง**
ระบบมีข้อมูลจริงอยู่ ดังนั้น `[]` = RLS บล็อกได้ผล → **ข้อมูลธุรกิจไม่รั่วให้คนนอก** ✅

> **หมายเหตุความซื่อสัตย์:** ดิฉันลอง probe ฝั่ง INSERT ด้วย payload ที่ผิด type โดยตั้งใจ (ไม่มีแถวถูกเขียน — ยืนยันแล้วว่าไม่มีขยะตกค้าง) ได้ `22P02 invalid input syntax` ไม่ใช่ `42501 permission denied`
> **แต่ผลนี้สรุปอะไรไม่ได้** เพราะ Postgres แปลง type ก่อนตรวจ RLS policy — จึงไม่ได้บอกว่า RLS ยอมให้ anon เขียนหรือไม่ ดิฉันจึงไปอ่าน policy จริงแทน (ผลด้านบน)

---

## ⚠️ ความเสี่ยงที่ stub มองไม่เห็น — ต้องตรวจหลังรัน migration  ⚪ [อนุมาน]

**`NUMERIC(9,6)` — PostgREST คืนเป็น JS number หรือ string?**
ถ้าคืนเป็น **string** โค้ดจะพังทันที 2 จุด:
- `jobs-nearby/page.tsx:180` → `loc.lat.toFixed(4)` → **TypeError: toFixed is not a function**
- `haversineKm(...)` → คำนวณกับ string ได้ `NaN` ทั้งหน้า

stub ของดิฉันคืนเป็น number จึงไม่มีทางจับบั๊กคลาสนี้ได้เลย

**หลักฐานทางอ้อมที่ทำให้เชื่อว่าน่าจะเป็น number:** `fuel_events.fuel_liters` เป็น `NUMERIC(8,3)` และโค้ด production ที่ใช้งานอยู่ทุกวันนี้เรียก `fe.fuel_liters.toFixed(2)` (`fuel/page.tsx:619`) ถ้าคืนเป็น string หน้านั้นจะพังไปแล้ว
**แต่นี่คือการอนุมานจาก precedent ไม่ใช่การทดสอบ** — ต้องยืนยันจริงหลัง migration ลง

**ความเสี่ยงอื่นที่ stub มองไม่เห็น:**
- query ที่ผสม `.in()` + `.is()` + `.order()` ทำงานตรงตามที่หน้าคาดหวังบน PostgREST จริงไหม
- `.is('assigned_driver_id', null)` กับค่า NULL จริงใน Postgres
- การ insert `truck_locations` จริงจะติด constraint `source CHECK (...)` หรือ RLS หรือไม่

---

## ✅ สิ่งที่ยังยืนยันได้ว่าดี

**🟡 ระดับโค้ด (เชื่อได้):**
- `haversineKm` สูตรถูกต้องตามมาตรฐาน (R=6371, atan2) — เทียบกับ implementation แยกที่เขียนใน Python แล้วตรงทุกหลัก
- โครงสร้างหน้าอ่านง่าย แยก `useMemo` เป็นชั้นๆ ชัดเจน
- มี empty state ครบทั้ง 2 แบบ + แถบเตือนงานไม่มีพิกัด — คิดถึง edge case มาแล้ว
- เขียน responsive ไว้ในหน้าตัวเอง (`flex-wrap`, `min-w-[200px]`)
- `PATCH /api/jobs/[id]` ส่งพิกัดผ่านได้ด้วย `...rest` (แก้งานเดิมแล้วพิกัดไม่หาย)

**🔵 ระดับ stub (บอกได้แค่ว่าโค้ด React ถูก ถ้า DB คืนข้อมูลตามที่คาด):**
- ตัวกรองรัศมี 20→J2 · 50→J2,J1 · 100→J4,J2,J1 · ไม่จำกัด→ครบ 4
- สลับเรียง "ใกล้ที่สุด" ลำดับเปลี่ยนตามระยะทาง
- เลือกแถวพิกัดล่าสุดเมื่อคนขับมีหลายแถว
- กรองงานที่จัดรถแล้วออก
- round-trip บันทึกตำแหน่ง → SPA navigate → เห็นในหน้า nearby
- console ไม่มี exception (มีแค่ `favicon.ico` 404 ซึ่งมีมาก่อนแล้ว)

---

## สิ่งที่ต้องทำก่อน push

| ลำดับ | รายการ | ใคร |
|---|---|---|
| 1 | 🔴 รัน migration 014 บน production แล้ว re-probe ยืนยัน 200 | CEO / Dev1 |
| 2 | 🐛 แก้ BUG 1 — `totalKm = null` เมื่อไม่มีพิกัดปลายทาง | Dev1 |
| 3 | 🐛 แก้ BUG 2 — validate ช่วง lat/lng ทั้ง UI และ API | Dev1 |
| 4 | 🟠 ใส่ label ให้ช่องพิกัด 4 ช่องที่ `/jobs` | Dev1 |
| 5 | ⛔ **QA รอบ 2 บนของจริง** — ล็อกอินจริง, ข้อมูลจริง, เช็ค NUMERIC, เช็ค role ทีละ role | Fern |
| 6 | 🟡 (ตามได้ทีหลัง) limit query, รูปแบบพิกัด, role gating ที่ UI | Dev1 |

**ข้อ 5 สำคัญไม่แพ้ข้ออื่น** — ผลรอบนี้ยืนยันได้แค่ว่า "โค้ดฝั่ง React ถูก" ยังไม่มีอะไรยืนยันว่าฟีเจอร์ทำงานบน stack จริง
ขอ credential สำหรับ QA (หรือ seed test user ที่ลบทิ้งได้) แล้วดิฉันจะตรวจซ้ำให้ครบทุกข้อที่ติดป้าย 🔵 และ ⚪

---

## วิธีทดสอบโดยละเอียด

- 🟢 ตรวจ DB production ด้วย REST API + anon key — **อ่านอย่างเดียว** ยกเว้น probe INSERT 2 ครั้งที่ใช้ payload ผิด type โดยตั้งใจ (ไม่มีแถวถูกเขียน ยืนยันด้วย `?origin=eq.__fern_rls_probe__` → `[]`)
- 🔵 ก๊อปโปรเจกต์ไป `%TEMP%\qa3\app` + junction `node_modules` + ลบ `middleware.ts` + stub `lib/supabase/{client,server}.ts`
  - stub เขียนให้ `.eq()/.is()/.in()/.order()/.insert()` กรองและเรียงจริง — **แต่เป็น semantics ที่ดิฉันตีความเอง ไม่ใช่ของ PostgREST**
  - ข้อมูลทดสอบทั้งหมด (คนขับ 3, งาน 6, พิกัดทุกจุด) ดิฉันแต่งขึ้นเอง
- ขับ Chrome headless ผ่าน CDP ที่ 375 / 768 / 1280 / 1440px — กดปุ่ม เปลี่ยน select กรอกฟอร์มจริง
- repo จริงไม่ถูกแก้: `git diff --stat` เท่าเดิม 8 ไฟล์ 154+/17-
- process: dev server **PID 45848** (ปิดแล้ว) + headless chrome (สคริปต์ปิด PID ตัวเองตอนจบ) — Chrome ของเจ้าของ 28 process ไม่ถูกแตะ

### ภาพหลักฐาน (ทุกภาพเป็นหน้าจอจาก stub)
| ไฟล์ | คำอธิบาย |
|---|---|
| `qa-shots/nearby/nearby-1280.png` | /jobs-nearby @1280 — เห็นบั๊กแถบเขียว |
| `qa-shots/nearby/nearby-375.png` | @375 — ตัวควบคุมนอกจอ |
| `qa-shots/nearby/jobs-edit-coords.png` | ฟอร์มแก้ไขงาน — ช่องพิกัดไม่มี label |
| `qa-shots/nearby/drivers-loc-form.png` | ฟอร์มกรอกตำแหน่งรถ |
| `qa-shots/nearby/roundtrip-nearby.png` | หลังบันทึกตำแหน่งแล้วกลับมาหน้านี้ |
| `qa-shots/nearby/nearby-sortdist-1280.png` | สลับเรียง "ใกล้ที่สุด" |

---

## บันทึกการแก้ไขรายงาน

**ฉบับแรก (30/08) มีปัญหา 5 ข้อ ที่แก้ในฉบับนี้:**
1. ซ่อนข้อจำกัดเรื่อง stub ไว้ท้ายรายงาน 239 บรรทัด → **ย้ายขึ้นหัวรายงาน**
2. ใช้คำว่า "ทดสอบจริง" / "ยืนยันจริง" กับผลที่มาจาก stub → **ติดป้ายระดับหลักฐานทุกข้อ**
3. เคลม "ค่าผิดลง DB จริงได้" ทั้งที่ไม่ได้ทดสอบ → **แก้เป็น [อนุมานจาก DDL]**
4. เรื่อง role ตรวจชั้นเดียว (frontend) → **เพิ่มชั้น backend (อ่าน RLS policy) + ระบุสิ่งที่ยังไม่ได้ทดสอบ**
5. ไม่ได้พูดถึงความเสี่ยง `NUMERIC` number-vs-string ที่ stub มองไม่เห็น → **เพิ่มเป็นหัวข้อแยก**
