# UX/UI Audit — truck-logistics-os (แอปรถพ่วง)

**ผู้ตรวจ:** Fern (QA Consultant)
**วันที่:** 28 สิงหาคม 2026
**ถึง:** Shino (Director) → CEO
**ระบบ:** Next.js 14 App Router + TypeScript + Supabase · หจก.ณสิริทรัพย์ การเกษตร

---

## 0. วิธีตรวจ (Methodology)

| ขั้นตอน | รายละเอียด |
|---|---|
| อ่านโค้ด | ทุกไฟล์ใน `src/app/(main)/*`, `src/components/*`, `src/lib/*`, `middleware.ts`, `globals.css` |
| เปิดเว็บจริง | `next dev` + Chrome headless ผ่าน Chrome DevTools Protocol |
| Breakpoints | **375px** (iPhone SE/12 mini) · **768px** (iPad portrait) · **1280px** (desktop) |
| หน้าที่วัดจริงหลังล็อกอิน | `/jobs`, `/advances`, `/fuel`, `/trips`, `/reports` + เปิด modal จริง |
| มาตรฐานอ้างอิง | WCAG 2.1 AA/AAA, Apple HIG, Material Design 3 |

### การล็อกอินเข้าหน้าจริง
เครื่องนี้ไม่มี Docker → รัน Supabase local ไม่ได้ และดิฉัน**เลือกที่จะไม่สร้าง test user บน production auth** เพราะไม่มี service-role key ไว้ลบทิ้งภายหลัง (จะทิ้งขยะไว้ในระบบจริงของบริษัท)

แทนที่จะข้ามการทดสอบ ดิฉัน**ก๊อปโปรเจกต์ทั้งชุดไปไว้ในโฟลเดอร์ชั่วคราว** (`%TEMP%\qa2\app`, link `node_modules`) แล้วในสำเนานั้นเท่านั้น: ลบ `middleware.ts`, stub `lib/supabase/{client,server}.ts` ให้คืนข้อมูลตัวอย่าง

ผลคือ **React component หน้าจริงทุกตัว, CSS ที่ compile จริง, routing จริง** ถูก render และวัดในเบราว์เซอร์จริง
> ✅ repo จริงไม่ถูกแตะแม้แต่บรรทัดเดียว (`git diff --stat` เท่าเดิม 8 ไฟล์ที่ค้างอยู่ก่อนแล้ว)
> ✅ ไม่มีการเชื่อมต่อ/แก้ไข Supabase production
> ⚠️ ข้อมูลที่แสดงเป็นข้อมูลจำลอง — **ตัวเลข layout/pixel เป็นของจริง** แต่จำนวนแถวไม่ใช่ข้อมูลจริงของบริษัท

---

## 🔴 CRITICAL — ต้องแก้ก่อนเปิดใช้จริง

### C1. แอปใช้บนมือถือไม่ได้เลย (ไม่ใช่ "ใช้ยาก" — คือใช้ไม่ได้)

**หลักฐานวัดจากหน้า `/jobs` จริง @375px:**

| ค่า | ผลวัด |
|---|---|
| `<main>` margin-left | **256px** |
| `<main>` ความกว้างที่เหลือ | **119px** |
| `<main>` overflow-x | **hidden** |
| `document.scrollWidth` | **375px** |
| ปุ่ม/ช่องกรอกที่ถูกตัดออกนอกจอ | **หลายสิบจุด** |

**ต้นเหตุ:** `Sidebar.tsx:91` = `fixed inset-y-0 left-0 w-64` และ `(main)/layout.tsx:24` = `flex-1 ml-64 ... overflow-x-hidden`
ทั้งสองไฟล์**ไม่มี responsive class แม้แต่ตัวเดียว** และไม่มีปุ่มแฮมเบอร์เกอร์

**จุดที่ร้ายที่สุด:** `scrollWidth = 375` เท่ากับ viewport พอดี เพราะ `overflow-x-hidden` **ตัดทิ้ง** ไม่ใช่แค่ซ่อน → user **เลื่อนไปหาของที่หายไปไม่ได้เลย**

**ปุ่มที่วัดได้ว่าอยู่นอกจอ (หน้า /jobs @375):**

| ปุ่ม | ตำแหน่ง x | สถานะ |
|---|---|---|
| รีเฟรช | 373–423 | ❌ นอกจอ |
| เพิ่มงาน | 431–510 | ❌ นอกจอ |
| ช่องกรองวันที่ (จาก) | 349–501 | ❌ นอกจอ |
| ช่องกรองวันที่ (ถึง) | 525–677 | ❌ นอกจอ |
| **ส่งงานแล้ว** (เลื่อนสถานะ) | 313–400 | ❌ นอกจอ |
| **จัดรถแล้ว** | 382–464 | ❌ นอกจอ |
| **แก้ไข** | 406–432 | ❌ นอกจอ |
| **ลบ** | 438–464 | ❌ นอกจอ |

→ บนมือถือ **ทำอะไรกับงานไม่ได้เลยสักอย่าง** เห็นแค่เมนู

**ผลวัดหน้าอื่น @375px:** ทุกหน้า `main` = 119px เหมือนกันหมด
`/advances` = 42 ปุ่มถูกตัด · `/trips` = **89** ปุ่มถูกตัด · `/fuel` = 3 · `/reports` = 3

**นี่คือกลุ่มผู้ใช้หลัก** — คนขับหน้างานและพนักงานลานใช้มือถือ

**แก้:**
```tsx
// (main)/layout.tsx  — ลบ overflow-x-hidden ออกด้วย (มันซ่อนอาการไว้)
<main className="flex-1 ml-0 lg:ml-64 min-h-screen">

// Sidebar.tsx
<aside className="fixed inset-y-0 left-0 w-64 z-40 transform
                  -translate-x-full lg:translate-x-0 transition-transform duration-200">
```
\+ ปุ่มแฮมเบอร์เกอร์ + backdrop สำหรับ `<lg` + ปิด sidebar อัตโนมัติเมื่อกดเมนู

---

### C2. แท็บเล็ต 768px: กด "ปฏิเสธ" คำขอเบิกเงินไม่ได้ แต่กด "อนุมัติ" ได้

**วัดจากหน้า `/advances` จริง @768px:**

| ปุ่ม | ตำแหน่ง x | กดได้? |
|---|---|---|
| อนุมัติ | 660–725 | ✅ **ได้** |
| **ปฏิเสธ** | 731–800 | ❌ **ไม่ได้** |
| **ลบ** | 806–832 | ❌ **ไม่ได้** |

ตารางกว้าง 567px ยื่นไปถึง x=848 (เกินจอ 80px) อยู่ใน wrapper `overflow: hidden` → ส่วนที่เกินถูกตัดถาวร

**นี่อันตรายกว่าการที่ปุ่มหายทั้งแถว** — พนักงานเห็นแค่ปุ่ม "อนุมัติ" ปุ่มเดียว ถ้ารีบก็จะกดอนุมัติ ทั้งที่ตั้งใจจะปฏิเสธ **เป็นช่องทางให้เงินรั่วโดยไม่ตั้งใจ**

---

### C3. ปุ่มแก้ไข/ลบ ในตารางเที่ยววิ่ง = ล่องหนบนทัชสกรีน

`TripTable.tsx:111` → `opacity-0 group-hover:opacity-100`

**วัดจากหน้า `/trips` จริง @375px: พบ control ที่ `opacity: 0` จำนวน 12 ตัว**

มือถือ/แท็บเล็ต**ไม่มี hover** → ปุ่มโปร่งใส 100% ตลอดเวลา แก้ไข/ลบรายการไม่ได้เลย และ user ไม่มีทางรู้ว่ามีปุ่มอยู่

ขนาดไอคอน `w-4 h-4` = **16×16px** — ต่ำกว่า WCAG 2.5.8 (AA, ขั้นต่ำ 24×24), Apple HIG 44pt, Material 48dp

**แก้:** ลบ `opacity-0 group-hover:opacity-100` ออก ใช้ `text-slate-400 hover:text-blue-600` และเพิ่ม padding เป็น `p-2.5` ให้ hit area ≥44×44px

---

## 🟠 HIGH

### H1. ตาราง 12 จาก 16 ตัว ถูกตัดบนจอเล็ก — **แก้ครบตามตารางนี้เท่านั้น**

> ⚠️ รอบแรกดิฉันนับพลาด (รายงานว่า "9 ตาราง ใน 6 หน้า") เพราะใช้ `grep -c overflow-x-auto` ระดับ**ไฟล์** พอเจอคำนี้ 1 ครั้งใน `reports/page.tsx` เลยตัดทั้งหน้าออก ทั้งที่หน้านั้นมี 5 ตารางและมี wrapper แค่ตัวเดียว
> รอบนี้ตรวจ**ทีละตาราง** โดยไล่ ancestor ขึ้นไปหา wrapper จริง

| ไฟล์ : บรรทัด | wrapper ที่เจอ | สถานะ |
|---|---|---|
| `advances/page.tsx:186` | `overflow-hidden` | ❌ ตัดทิ้ง |
| `customers/page.tsx:550` | `overflow-hidden` | ❌ ตัดทิ้ง |
| `customers/page.tsx:608` | `overflow-hidden` | ❌ ตัดทิ้ง |
| `dashboard/page.tsx:358` | `overflow-x-auto` | ✅ ผ่าน |
| `expenses/page.tsx:155` | `overflow-hidden` | ❌ ตัดทิ้ง |
| `payroll/page.tsx:475` | `overflow-hidden` | ❌ ตัดทิ้ง |
| `payroll/page.tsx:514` | `overflow-hidden` | ❌ ตัดทิ้ง |
| `payslip/page.tsx:332` | ไม่มี | ❌ |
| **`reports/page.tsx:462`** | **ไม่มี** | ❌ **(รอบแรกตกไป)** |
| **`reports/page.tsx:525`** | **ไม่มี** | ❌ **(รอบแรกตกไป)** |
| `reports/page.tsx:705` | `overflow-x-auto` | ✅ ผ่าน |
| **`reports/page.tsx:809`** | **`overflow-hidden`** | ❌ **(รอบแรกตกไป)** |
| **`reports/page.tsx:932`** | **`overflow-hidden`** | ❌ **(รอบแรกตกไป)** |
| `trips/page.tsx:731` | ไม่มี | ❌ |
| `trips/page.tsx:890` | `overflow-auto` | ✅ ผ่าน |
| `components/trips/TripTable.tsx:38` | `overflow-x-auto` | ✅ ผ่าน |

**รวม: 16 ตาราง — ผ่าน 4, ต้องแก้ 12 ใน 8 ไฟล์**

หมายเหตุ: `overflow-hidden` **แย่กว่า** ไม่มี wrapper เพราะตัดทิ้งแน่นอน ส่วน "ไม่มี wrapper" ก็ยังโดน `<main>` ที่เป็น `overflow-x-hidden` ตัดอยู่ดี → **ทั้ง 12 ตัวเข้าไม่ถึงเหมือนกันหมด**

**แก้:** ครอบทุกตารางด้วย `<div className="overflow-x-auto">` (ถ้าการ์ดต้องมีมุมโค้ง ให้ใส่ `overflow-hidden` ที่การ์ดชั้นนอก แล้ว `overflow-x-auto` ที่ div ชั้นในที่ห่อ `<table>` โดยตรง) — `TripTable.tsx:38` ทำถูกอยู่แล้ว ใช้เป็นต้นแบบได้

---

### H2. คลาส `.label-text` ไม่มีอยู่จริงใน CSS — ใช้อยู่ 27 จุด

`grep -c "label-text"` ใน CSS ที่ **compile จริง** = **0** (ส่วน `.form-label` มีจริงที่ `globals.css:84`)

**ยืนยันบนแอปจริง** (เปิด modal "เพิ่มรายการเบิกเงิน" ที่ `/advances` แล้ววัด computed style):

| | `.label-text` (พัง) | `.form-label` (ถูก) |
|---|---|---|
| display | inline | block |
| font-size | **16px** | 14px |
| font-weight | **400** | 500 |
| margin-bottom | **0px** | 4px |

> 🔧 **แก้ข้อมูลจากรายงานรอบแรก:** ดิฉันเคยเขียนว่า `display:inline` ทำให้ label "ไปเบียดกับ input บรรทัดเดียวกัน" — วัดจริงแล้ว**ไม่ใช่** (`gap: 2px`, `sameLine: false`) เพราะ input เป็น block จึงขึ้นบรรทัดใหม่อยู่ดี
> อาการจริงคือ: **ตัวใหญ่กว่า (16 vs 14px), ไม่หนา (400 vs 500), และชิดกล่อง (2px แทน 4px+)** — ฟอร์มใน modal ของ `advances`, `expenses`, `fuel`, `jobs` หน้าตาไม่เหมือน `trips`, `drivers`, `customers`, `settings`, `login`

**แก้ (1 บรรทัด) ใน `globals.css`:**
```css
.label-text { @apply block text-sm font-medium text-slate-700 mb-1; }
```
หรือดีกว่า — replace `label-text` → `form-label` ทั้ง 4 ไฟล์ ให้เหลือคลาสเดียว

---

### H3. คลิกพื้นหลัง = ข้อมูลที่กรอกหายทันที ไม่มีเตือน

**8 modals** ใส่ `onClick={onClose}` ที่ backdrop:
`advances:311` · `customers:647, 708, 789` · `expenses:233` · `fuel:464` · `jobs:522, 693`

และ **ไม่มีที่ไหนรองรับปุ่ม Escape เลยทั้งแอป** (มีแค่ `onKeyDown` Enter ที่ `settings:138, 165`)

กรอกงานใหม่ 12 ช่อง → นิ้วโดนขอบจอ → หายหมด ไม่มีคำถาม "ต้องการทิ้งข้อมูลหรือไม่?"

**แก้:** เก็บ `initialForm` ไว้เทียบ ถ้า dirty ให้ถามก่อนปิด + เพิ่ม `useEffect` ดัก `Escape` (ให้ถามเหมือนกัน) + focus trap ใน modal

---

### H4. เรื่องเงินและสถานะ = คลิกเดียว ไม่มียืนยัน ไม่มี undo

| การกระทำ | มี confirm? | ไฟล์ |
|---|---|---|
| จ่ายเงิน (`pay`) | ✅ มี | `advances:87` |
| **อนุมัติเงินเบิก** (`approve`) | ❌ **ไม่มี** | `advances:87` |
| **ปฏิเสธเงินเบิก** (`reject`) | ❌ **ไม่มี** | `advances:87` |
| **เลื่อนสถานะงาน** (รวม "ปิดงาน") | ❌ **ไม่มี** | `jobs:124` |

`advances/page.tsx:87` → `if (action === 'pay' && !confirm(...))` ครอบแค่ `pay` เท่านั้น
`jobs/page.tsx:124` → `advanceStatus()` ไม่มี confirm เลย

ซ้ำร้าย บนเดสก์ท็อปปุ่ม "เลื่อนสถานะ" อยู่ห่างจากปุ่ม "ลบ" แค่ ~6px

การปฏิเสธคำขอเงินของคนขับจากการกดพลาดครั้งเดียว แล้วย้อนไม่ได้ — เป็นเรื่องความสัมพันธ์กับคน ไม่ใช่แค่ข้อมูลผิด

**แก้:** confirm ทุก action ที่แตะเงินหรือเปลี่ยนสถานะแบบย้อนไม่ได้ + เพิ่มปุ่ม "ย้อนสถานะ" ในหน้า jobs + เว้นระยะปุ่มอันตรายอย่างน้อย 16px

---

### H5. Contrast ต่ำกว่ามาตรฐาน 19 จุด (WCAG 2.1 AA §1.4.3 ต้องการ 4.5:1)

| อัตราส่วนที่วัดได้ | สี | ใช้กับ |
|---|---|---|
| **1.48 : 1** | `text-slate-300` | เครื่องหมาย `/` คั่นเลขไมล์ |
| **2.34 : 1** | slate-400 บน slate-100 | ตัวเลขบอกว่ารูปไหน**ยังไม่ได้ส่ง** |
| **2.56 : 1** | `text-slate-400` (#94a3b8) | เลขที่งาน · **วันที่ทุกหน้า** · เส้นทาง · หมายเหตุ · ทะเบียนรถ · "12 รายการ" |
| **3.00 : 1** | green-600 บน green-100 | ตัวเลขลำดับรูป |
| **3.11 : 1** | orange-600 บน orange-100 | ป้ายสถานะ "ต้องตรวจสอบ" |
| **3.35 : 1** | orange-600 บน orange-50 | ป้าย "เครดิต ครบกำหนด" |
| **3.56 : 1** | `text-orange-600` | **ยอดเงินค่าน้ำมัน** |
| **3.76 : 1** | white บน red-500 | ตัวเลข badge แจ้งเตือนใน sidebar |
| 4.34–4.49 : 1 | slate-500 / red-600 | ป้าย "Bank", "ผิดปกติ", อีเมล user |

`text-slate-400` ไม่ได้ใช้กับของประดับ — มันใช้กับ **วันที่และเลขที่งาน** ซึ่งเป็นข้อมูลที่ต้องอ่านจริง คนขับอายุ 40+ กลางแดดจ้าอ่านไม่ออก

**แก้:** เปลี่ยน `text-slate-400` → `text-slate-500` (#64748b = **4.76:1 ผ่าน AA**) ทุกจุดที่เป็นข้อมูลจริง เก็บ slate-400 ไว้เฉพาะ placeholder · ป้ายสถานะเปลี่ยนเป็นเฉด `-700` (เช่น `text-orange-700` บน `bg-orange-100` = 4.8:1)

---

### H6. รูปน้ำมัน 3 รูป จับคู่ตาม "ลำดับที่ส่ง" อย่างเดียว

`api/line/webhook/route.ts` → `groupFuelPhoto()` เติมตามช่องว่างที่เหลือ: รูปแรก = หัวจ่าย, สอง = ใบเสร็จ, สาม = เลขไมล์

คนขับส่งสลับลำดับ (เกิดบ่อยมากกับคนไม่ถนัดมือถือ) → **OCR อ่านเลขไมล์ไปใส่ช่องยอดเงิน** แต่บอทยังตอบยืนยันอย่างมั่นใจว่า *"📸 ได้รับรูปที่ 2/3 (ใบเสร็จ)"* และ**ไม่มีคำสั่งให้ส่งใหม่หรือยกเลิก**

**แก้:** ให้บอทถามยืนยันก่อนบันทึกแต่ละรูป หรืออย่างน้อยเพิ่มคำสั่ง `!เริ่มใหม่` และให้หน้า `/fuel` สลับรูปได้ตอนตรวจสอบ

> 👍 **ขอชมว่า flow LINE ของคนขับออกแบบดีมาก** — บอกเป็นขั้น 1/3, 2/3, 3/3, มีตัวอย่างข้อความ (`!เบิก จง 2000 ค่าข้าว`), เช็ควงเงินพร้อมบอกยอดคงเหลือเป็นตัวเลข ตรงนี้เหมาะกับคนไม่ถนัดเทคโนโลยีจริง **เก็บไว้ทั้งหมด**

---

## 🟡 MEDIUM

### M1. ช่องตัวเลข 30 จาก 34 ช่อง ไม่มี `min="0"`
กรอกยอดเงิน/น้ำหนัก/ลิตร ติดลบได้ ไม่มีอะไรกันทั้ง client และ UI
**แก้:** เพิ่ม `min="0"` ทุกช่องที่เป็นจำนวนเงิน น้ำหนัก ลิตร ระยะทาง เลขไมล์

### M2. ไม่มีการตรวจ "ไมล์ปลาย < ไมล์ต้น"
`lib/utils.ts:109` → `calcDistance = Math.max(0, end - start)` — **กลืนความผิดพลาดเงียบๆ**
ถ้าคนขับพิมพ์สลับเลข (453020 → 435020) ระยะทางกลายเป็น 0 กม. ไม่มีใครรู้ ไม่มีคำเตือน และตัวเลขนี้ไหลต่อไปที่รายงานรายเดือน
**แก้:** แสดง error `"ไมล์ปลายน้อยกว่าไมล์ต้น กรุณาตรวจสอบ"` แทนการปัดเป็น 0

### M3. Realtime ทำให้จอขาวทั้งหน้า
`fuel:33-60`, `trips:103`, `dashboard:89-90` — subscription เรียก `loadData()` ที่ `setLoading(true)` แล้วหน้าขึ้น full-screen spinner
พอคนขับส่งรูปเข้ามาระหว่างที่ออฟฟิศกำลังตรวจงาน **หน้าจอกลายเป็นสปินเนอร์เต็มจอ** งานที่ดูค้างไว้หาย ตำแหน่ง scroll หาย
**แก้:** แยก `setRefreshing` ออกจาก `setLoading` แล้วโชว์ spinner เล็กมุมขวาบนแทน (โหลดครั้งแรกเท่านั้นที่ใช้ full-screen)

### M4. เมนู "ชิปปิ้ง" กดได้แต่เป็นทางตัน
`Sidebar.tsx:41` ใส่ `/import` ไว้ใน `PHASE_AVAILABLE` แต่ `import/page.tsx` ขึ้นว่า "กำลังพัฒนา"
**แก้:** เอา `/import` ออกจาก `PHASE_AVAILABLE` — ป้าย "เร็วๆ นี้" ที่เขียนโค้ดรองรับไว้แล้ว (`Sidebar.tsx:122`) จะทำงานเอง

### M5. ปุ่ม icon-only ไม่มีชื่อให้ screen reader
ปุ่มรีเฟรชหน้า `/jobs` (`jobs:168`) **ไม่มีทั้ง `title` และ `aria-label`** → accessible name ว่างเปล่า
ปุ่มลบ/แก้ไขมีแค่ `title` ซึ่ง**บนทัชสกรีนไม่แสดงเลย** (ไม่มี hover)
**แก้:** ใส่ `aria-label` ทุกปุ่มที่มีแต่ไอคอน

### M6. `<label>` ไม่ผูกกับ `<input>` — ผิด WCAG 1.3.1 / 3.3.2
ทั้งแอปเขียน `<label className="form-label">อีเมล</label>` แล้ววาง input ต่อ ไม่มี `htmlFor`/`id` และไม่ครอบ input
**วัดจากหน้า `/trips` จริง: 21 จาก 22 input ไม่มี label ผูก** · `/advances` 2/2 · `/fuel` 2/2 · `/login` 2/2
ผลข้างเคียง: แตะที่ข้อความ label แล้ว input ไม่ focus — เสีย tap target ฟรีๆ บนมือถือ
**แก้:** ใส่ `id` + `htmlFor` เป็นคู่ หรือครอบ input ไว้ใน `<label>`

### M7. Tap target เล็กกว่ามาตรฐานจำนวนมาก
วัดที่ 375px: `/trips` **51 จุด** · `/advances` 27 · `/fuel` 18 · `/reports` 17 ที่มีขนาด < 44×44px
Apple HIG = 44pt · Material Design 3 = 48dp · WCAG 2.5.5 (AAA) = 44px · WCAG 2.5.8 (AA) = 24px ขั้นต่ำ
**แก้:** ปุ่มไอคอนใช้ `p-2.5` ขึ้นไป, `.form-input` เพิ่มเป็น `py-2.5` (จาก 38px → 44px)

---

## 🟢 LOW

### L1. ใช้ `alert()` 17 จุด / `confirm()` 11 จุด
ทั้งที่ `TripTable.tsx:162-188` มี modal ยืนยันที่ออกแบบสวยอยู่แล้ว
`alert()` บนมือถือบล็อกทั้งจอและหน้าตาไม่เข้ากับดีไซน์ที่เหลือ
**แก้:** สกัด `<ConfirmDialog>` จาก TripTable ออกมาเป็น shared component แล้วใช้ทั้งแอป

### L2. ปุ่มไม่มี custom focus indicator
วัดได้ `outline: auto 1px` (ค่า default เบราว์เซอร์) — บนพื้นหลัง navy `#1E3A5F` ของ sidebar แทบมองไม่เห็น
(`.form-input` มี `focus:ring-2` อยู่แล้ว ✅ แต่ `.btn-primary` / `.sidebar-link` ไม่มี)
**แก้:** เพิ่ม `focus-visible:ring-2 focus-visible:ring-yellow-400 focus-visible:ring-offset-2`

### L3. Dead code ใน `src/lib/line/webhook.ts`
`detectTextIntent`, `parseAdvanceRequest`, `parseOdometerReading` **ไม่ถูกเรียกใช้ที่ไหนเลย**
> ตอนแรกดิฉันกังวลว่า regex `/รับงาน|โอเค|\bok\b/` จะทำให้คนขับพิมพ์ "โอเค" ในแชทแล้วกดรับงานโดยไม่ตั้งใจ — **ตรวจแล้วพบว่าไม่ได้ต่อสายไว้ จึงไม่ใช่ปัญหาจริงในตอนนี้**
**แก้:** ลบทิ้ง หรือถ้าจะเปิดใช้ในอนาคต ต้องรื้อ regex ให้รัดกุมก่อน

---

## สรุปสำหรับ CEO

**เดสก์ท็อป 1280px แอปนี้ทำได้ดีจริง** — วัดแล้ว horizontal overflow = 0, layout สะอาด, ภาษาไทยชัดเจน, ระบบ badge realtime ใน sidebar มีประโยชน์ และ flow LINE ของคนขับคิดมาดีมาก

**ปัญหาเกือบทั้งหมดกระจุกอยู่ที่สาเหตุเดียว: ไม่เคยถูกออกแบบสำหรับจอเล็ก**
ทั้งโปรเจกต์ 9,130 บรรทัด มี responsive class (`sm:`/`md:`/`lg:`) รวมกันแค่ **15 ตัว** และ **15 จาก 22 ไฟล์ไม่มีเลยสักตัว**

### ลำดับการแก้ที่แนะนำ

| ลำดับ | ทำอะไร | เวลาโดยประมาณ | ได้อะไร |
|---|---|---|---|
| **1** | C1 + C2 + C3 + H1 (mobile shell, ตาราง 12 ตัว, ปุ่ม hover-only) | ~1–1.5 วัน | ปลดล็อกผู้ใช้กลุ่มใหญ่ที่สุด และปิดช่องเงินรั่วจาก C2 |
| **2** | H2 + H3 + H4 (label-text 1 บรรทัด, กันข้อมูลหาย, confirm เรื่องเงิน) | ~ครึ่งวัน | คุ้มที่สุดต่อเวลาที่ลงแรง |
| **3** | H5 + H6 (contrast, ลำดับรูป OCR) | ~1 วัน | อ่านออกกลางแดด + กันข้อมูล OCR สลับช่อง |
| **4** | M1–M7 | ~1 วัน | กันกรอกผิด + accessibility |
| **5** | L1–L3 | ~ครึ่งวัน | ความสม่ำเสมอของงานดีไซน์ |

### ถ้าจะเปิดใช้จริงสัปดาห์นี้โดยยังไม่แก้

ต้องประกาศกับทีมให้ชัดว่า **"ระบบนี้ใช้บนคอมพิวเตอร์เท่านั้น"**
เพราะตอนนี้เปิดบนมือถือแล้วจะเห็นแค่เมนู กดอะไรไม่ได้เลย — และมันจะดูเหมือน**แอปเสีย** ไม่ใช่แอปที่ยังไม่รองรับมือถือ ซึ่งจะทำให้คนขับเลิกใช้ตั้งแต่วันแรก

---

## ภาคผนวก — ภาพหลักฐาน

| ไฟล์ | คำอธิบาย |
|---|---|
| `qa-shots/real-jobs-375.png` | **หน้า /jobs จริง @375px** — sidebar กิน 256px เหลือเนื้อหา 119px |
| `qa-shots/real-advances-375.png` | หน้า /advances จริง @375px |
| `qa-shots/real-advances-768.png` | หน้า /advances จริง @768px — ปุ่ม "ปฏิเสธ" ถูกตัด |
| `qa-shots/real-fuel-375.png` | หน้า /fuel จริง @375px |
| `qa-shots/real-trips-375.png` | หน้า /trips จริง @375px — 89 ปุ่มถูกตัด |
| `qa-shots/real-reports-375.png` | หน้า /reports จริง @375px |
| `qa-shots/real-modal-labeltext.png` | modal จริงที่ใช้ `.label-text` |
| `qa-shots/app-1280.png` | เดสก์ท็อป 1280px — ไม่มี overflow |
| `qa-shots/login-1280.png`, `login-375-cdp.png` | หน้า login (ผ่านทั้งสอง breakpoint ✅) |

---

## บันทึกความโปร่งใส

1. **repo จริงไม่ถูกแก้** — `git diff --stat` เท่าเดิม 8 ไฟล์ที่ค้างอยู่ก่อนเริ่มงาน
2. **ไม่มีการเชื่อมต่อหรือแก้ไข Supabase production** — ไม่ได้สร้าง test user ทิ้งไว้
3. **process ทั้งหมดปิดแล้ว** โดยระบุ PID เจาะจง (ดูรายการใน chat)
4. **ข้อผิดพลาดรอบแรกที่แก้ในฉบับนี้:** นับตารางขาด 4 ตัวในหน้า `reports` (ใช้ grep ระดับไฟล์), อธิบายอาการ `.label-text` เกินจริง, และรอบแรกวัดจาก harness ที่ประกอบเอง — ฉบับนี้วัดจากหน้าจริงทั้งหมด
