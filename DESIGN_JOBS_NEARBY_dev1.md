# ออกแบบฟีเจอร์ "งานใกล้รถพ่วง" (ยังไม่สร้างจริง — รอเจ้าของอนุมัติ)

เอกสารนี้เป็นแค่ **แผนออกแบบ** — ยังไม่แก้โค้ด ยังไม่รัน SQL อะไรทั้งสิ้น ขอให้เจ้าของอ่านแล้วบอกว่าโอเคไหม/อยากปรับตรงไหน ก่อนผมจะเริ่มลงมือจริง

## แนวคิดฟีเจอร์
ให้ระบบบอกได้ว่า "งาน (job) ไหนอยู่ใกล้รถพ่วงคันไหน" โดยดูจากตำแหน่งปัจจุบันของรถ เทียบกับพิกัดต้นทาง/ปลายทางของงานที่ยังไม่ได้จัดรถ เพื่อช่วยจัดรถได้เร็วขึ้น ไม่ต้องมานั่งกะด้วยสายตา

---

## (1) Schema ที่ต้องเพิ่ม

### 1.1 เพิ่มคอลัมน์พิกัดในตาราง `jobs` (ตารางเดิม ไม่ใช่สร้างใหม่)
```sql
ALTER TABLE jobs
  ADD COLUMN origin_lat       NUMERIC(9,6),
  ADD COLUMN origin_lng       NUMERIC(9,6),
  ADD COLUMN destination_lat  NUMERIC(9,6),
  ADD COLUMN destination_lng  NUMERIC(9,6);
```
- ทุกคอลัมน์เป็น **nullable** (ไม่บังคับกรอก) — งานเก่าที่ไม่มีพิกัดจะยังใช้งานได้ปกติ ไม่พัง
- `trips` (เที่ยววิ่ง) **ไม่ต้องแตะ** — เพราะฟีเจอร์นี้ใช้จับคู่ "งานที่ยังไม่ได้จัดรถ" กับ "รถที่ว่าง" ซึ่งอยู่ในตาราง `jobs` อยู่แล้ว (`jobs.origin`/`jobs.destination` เป็น text อยู่แล้ว แค่เพิ่มพิกัดคู่กัน)

### 1.2 ตารางใหม่ `truck_locations` — เก็บพิกัดสดของรถแต่ละคัน
```sql
CREATE TABLE truck_locations (
  id           UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  driver_id    UUID NOT NULL REFERENCES drivers(id),
  lat          NUMERIC(9,6) NOT NULL,
  lng          NUMERIC(9,6) NOT NULL,
  source       TEXT NOT NULL DEFAULT 'manual'
               CHECK (source IN ('gpsiam','line_share','manual')),
  recorded_at  TIMESTAMPTZ NOT NULL DEFAULT now(),
  created_at   TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX idx_truck_locations_driver_time
  ON truck_locations (driver_id, recorded_at DESC);
```
- **ทำไมผูกกับ `driver_id` ไม่ใช่ "trucks"**: โปรเจกต์นี้ไม่มีตาราง `trucks` แยก — ทะเบียนรถ (`license_plate`) เก็บอยู่ในตาราง `drivers` เลย (1 คนขับ = 1 คัน ในโครงสร้างปัจจุบัน) ดังนั้น "ตำแหน่งรถ" ก็คือ "ตำแหน่งของคนขับที่ผูกกับรถคันนั้น" — ไม่ต้องสร้างตารางรถแยกให้ซับซ้อนเพิ่ม
- เก็บเป็น **ประวัติ** (insert แถวใหม่ทุกครั้งที่มีพิกัดเข้ามา) ไม่ใช่ overwrite แถวเดียว เพราะเผื่ออยากดูย้อนหลังว่ารถวิ่งมาทางไหน — ตอนใช้งานจริงแค่ query แถวล่าสุดของแต่ละคนขับ (`ORDER BY recorded_at DESC LIMIT 1`)
- คอลัมน์ `source` ไว้บอกว่าพิกัดนี้มาจากไหน (GPSIAM / คนขับแชร์ผ่าน LINE / กรอกมือ) — ช่วย debug ได้ถ้าพิกัดดูแปลกๆ

### 1.3 ความสัมพันธ์กับตารางเดิม
```
drivers (1) ──< truck_locations (many)   -- ประวัติพิกัดของคนขับ/รถแต่ละคัน
jobs.assigned_driver_id ──> drivers.id    -- เดิมมีอยู่แล้ว ไม่ต้องแก้
```
ไม่มี breaking change กับความสัมพันธ์เดิมเลย เป็นการต่อเติมเท่านั้น

---

## (2) Scope การทำงาน — แบ่ง 3 เฟส

### เฟส 1 — ใส่พิกัดในงาน (jobs)
- ตอนสร้าง/แก้ไขงาน เพิ่มปุ่ม "ปักหมุด" หรือกรอกพิกัดต้นทาง/ปลายทางเอง
- หรือ auto-geocode: พิมพ์ชื่อสถานที่ (เช่น "เชียงราย", "แม่สอด") แล้วระบบแปลงเป็นพิกัดให้อัตโนมัติผ่านบริการแปลงที่อยู่→พิกัด (เช่น Google Maps หรือ OpenStreetMap ฟรี) — ความแม่นยำจะได้แค่ระดับ "อำเภอ/ตำบล" ไม่ใช่จุดแม่นยำระดับหน้าบ้าน เพราะพิมพ์แค่ชื่อจังหวัด/อำเภอ
- งานเก่าที่ยังไม่มีพิกัดจะแสดงในฟีเจอร์ "งานใกล้ฉัน" ไม่ได้ (ต้องกรอกก่อน) — ไม่กระทบการทำงานอื่นของหน้า jobs เดิม

### เฟส 2 — รับพิกัดสดจากรถ (มี 2 ทางเลือก ขึ้นกับผล GPSIAM)
**กรณี A: ถ้า GPSIAM มี API ให้ดึงพิกัดได้**
→ เขียนงานเบื้องหลัง (cron ทุก 5-10 นาที) ดึงพิกัดจาก GPSIAM ตามทะเบียนรถ แล้วบันทึกลง `truck_locations` (source = 'gpsiam')

**กรณี B: ถ้า GPSIAM ไม่เปิด API ให้บุคคลภายนอกใช้ (fallback)**
→ เสนอทางที่เข้ากับระบบเดิมได้ง่ายที่สุด: **ให้คนขับส่งตำแหน่งผ่าน LINE ที่ใช้อยู่แล้ว**
- ระบบมี LINE Bot เชื่อมอยู่แล้ว (ตาราง `drivers.line_user_id`, webhook ที่ `src/app/api/line/webhook`)
- LINE มีปุ่ม "แชร์ตำแหน่ง" ในตัวอยู่แล้ว (ไม่ต้องสร้างแอป/หน้าเว็บใหม่ ไม่ต้องขอสิทธิ์ GPS เพิ่ม) — คนขับกดแชร์ตำแหน่งในแชท LINE บอทที่คุยอยู่แล้วได้เลย
- แก้ webhook ให้รับข้อความประเภท "location" (ตอนนี้รับแค่ text/image) แล้วบันทึกพิกัดนั้นลง `truck_locations` (source = 'line_share')
- ข้อจำกัด: ต้องให้คนขับกดแชร์เอง เป็นระยะๆ (ไม่ auto-update ตลอดเวลาเหมือน GPS tracker จริง)

*ผมแนะนำให้ทำ "ทางเลือก B" ไว้เป็นฐานเสมอ ต่อให้ GPSIAM มี API ก็ตาม เพราะเป็น fallback ที่ราคาถูกและใช้โครงสร้างเดิมที่มีอยู่แล้ว — ถ้า GPSIAM ใช้ได้จริงค่อยเพิ่มเป็นทางเลือกที่ 2 ทีหลัง*

### เฟส 3 — หน้า UI "งานใกล้ฉัน"
- หน้าใหม่ (หรือ tab ในหน้า jobs เดิม) แสดงรายชื่อรถพ่วงแต่ละคัน พร้อมงานที่ยังไม่จัดรถ (`status = 'new'` หรือ `'waiting_driver'`) เรียงจากใกล้ไปไกล
- คำนวณระยะทางแบบเส้นตรง (haversine formula — คำนวณในโค้ด JS ฝั่งเว็บ ไม่ต้องพึ่งบริการภายนอก ไม่มีค่าใช้จ่ายเพิ่ม) ไม่ใช่ระยะทางถนนจริง (จะคลาดเคลื่อนได้บ้างตามลักษณะถนน)
- มีช่องตั้ง "รัศมีค้นหา" (เช่น 20 / 50 / 100 กม. หรือไม่จำกัด) กรองงานให้เหลือเฉพาะที่อยู่ในระยะ

---

## (3) ประเมินผลกระทบ

| หัวข้อ | รายละเอียด |
|---|---|
| หน้า/ฟอร์มที่ต้องแก้ | `jobs` (ฟอร์มสร้าง/แก้งาน — เพิ่มช่องพิกัด), webhook LINE (รับ location message), หน้าใหม่ "งานใกล้ฉัน" |
| Breaking change กับของเดิม | **ไม่มี** — ทุกคอลัมน์ใหม่เป็น nullable, ตารางใหม่ไม่กระทบตารางเดิม, query เดิมของหน้า jobs/trips ทำงานเหมือนเดิมทุกอย่าง |
| ความเสี่ยงหลัก | ความแม่นยำพิกัด (geocode จากชื่อสถานที่ = หยาบ, LINE share = ต้องพึ่งคนขับกดเอง) — ต้องตั้งความคาดหวังกับผู้ใช้งานไว้ก่อนว่าไม่ใช่ GPS tracker เรียลไทม์แบบมืออาชีพ |

### เวลาโดยประมาณต่อเฟส (คร่าวๆ ทำงานจริง ไม่รวมรอ review/deploy)
- **เฟส 1** (พิกัดในงาน + geocode): ครึ่งวัน – 1 วัน
- **เฟส 2 กรณี A** (เชื่อม GPSIAM): ประเมินไม่ได้จนกว่าจะรู้ว่า GPSIAM มี API จริงไหม/เอกสารเป็นอย่างไร
- **เฟส 2 กรณี B** (LINE share ตำแหน่ง): 1 – 1.5 วัน
- **เฟส 3** (หน้างานใกล้ฉัน + haversine + รัศมี): 1 – 1.5 วัน

---

## สิ่งที่ต้องตัดสินใจก่อนเริ่มลงมือจริง
1. ยืนยันสมมติฐาน "1 คนขับ = 1 คัน" ยังถูกต้องอยู่ไหม (ถ้ามีคนขับสลับรถ ต้องคิด schema เพิ่ม)
2. อยากให้เริ่มจากเฟส 1 ก่อนเลยไหม (ทำได้ทันทีไม่ต้องรอผล GPSIAM)
3. รอผลตรวจ GPSIAM API ให้ชัดก่อนตัดสินใจเฟส 2 ว่าจะทำ A, B, หรือทำ B ไว้ก่อนเป็นฐานแล้วค่อยเสริม A ทีหลัง
